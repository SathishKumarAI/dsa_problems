# Backlog — what to build next, and why that order

One item = one `type/scope-slug` branch = one PR, squash-merged; check the item off **in the
same commit** as the work and add a row to `FEATURES.md`. Pick the top unchecked P0.

Legacy item numbers (`legacy/visualizer/docs/BACKLOG.md` #1–#52) are referenced where an item is
a port; the original implementation is the reference, not the spec — the React port may do less.

## P0 — parity that protects the pedagogy

| # | Item | Why | Size | Ref |
|---|---|---|---|---|
| B1 | ☑ **Unified walkthroughs** — a problem with a journey renders its Walkthrough tab from the engine (`MiniPlayer`: same generators, chip grammar and panels), capped by the ledger so it never shows an approach the learner has not earned; the hand-written frames for those three problems are deleted and a test forbids having both. Shipped 2026-09-05 (`feat/unify-walkthrough`). | Two sources of truth for the same problem drift; the journey is the richer one. | S | PRD open question 1 |
| B2 | ☑ **UI smoke test** — `npm run test:ui`: `vite preview` + the system Chrome over CDP, no new dependencies. 18 checks: every route renders with a clean console, the earn loop writes the ledger, deep links (honoured, followed in-app, ignored when locked), corner-case loading, `f` / `?`, settings persistence, 390 px. Shipped 2026-09-05 (`test/ui-smoke`); it caught a real deep-link bug on its first run. Not yet in CI (no CI exists). | Every UI branch was verified by hand over CDP; that does not scale. | M | legacy #21 |
| B3 | ☑ **Settings dialog** — speed, motion dial, code tab, reading column, reset prefs, copy/import progress JSON, erase progress. Shipped 2026-09-04 (`feat/shell-panels-help`). Still open: theme toggle (with B14), reduce-motion override. | The motion dial had no UI; export/import is the cross-device story until there is a backend. | M | legacy #5, #22, #45 |
| B4 | ☑ **Collapsible rails** — sidebar and reading column each close from a button at their foot into a thin rail that keeps the button; both states persist. Shipped 2026-09-04 (`feat/journey-focus-rails`). The `f` / `Esc` cycling half moved to B26. | The stage is the product; a learner mid-act should be able to remove everything else without losing play/speed. | S | legacy #41 |
| B5 | ☑ **`?` shortcuts dialog** from `lib/shortcuts.ts` (by scope). Shipped 2026-09-04. | The visualizer page listed keys as text; the journey page listed none. | S | legacy #13, #42 |
| B6 | ☑ **Single Number code challenge + the reveal it was missing** — `singleNumber(nums)` with six cases (n = 1, answer 0, loner last, negatives), XOR reference, four review items, a 2 001-element second set; plus a recap act, so the journey ends on a named reveal like the others. The harness learned a second answer shape (`answers: "value"`). Shipped 2026-09-05 (`feat/single-number-challenge`). | Two Sum had the "prove it" act; Single Number ended on XOR without asking the learner to write it — and never named what they earned. | S | — |
| B7 | ☑ **Merge-sort write pulse + discard fade** — `set` steps now mark `write` (mauve) instead of borrowing `swap`, and pulse `scaleY(0.72 → 1.06 → 1)` in 260 ms; discarded bars fade to 40 % instead of greying. Shipped 2026-09-05 (`feat/bars-write-pulse`). | The bars FLIP on swaps; merge sort's writes teleported, because a store in place has no motion for FLIP to show. | S | legacy #44 |
| B8 | ☑ **Catalogue masking** — a pattern name is hidden only while a journey that reveals it is *started and unfinished* (`Journey.reveals` → `lib/disclosure.ts`); sidebar rows, the home grid, the pattern page heading and the where-you-are line all obey it, and one click ("show names anyway", stored in `spoilers`) turns it off for good. Shipped 2026-09-05 (`feat/disclosure-mask`). | The one rule the pedagogy rests on had a known leak in the shell. | S | legacy #52 |

| B46 | ☑ **Batch 2 of the derived journeys — ten more** — shipped 2026-09-08 (`feat/derived-journeys-batch-2`): subarray-sum-k, longest-consecutive-run, first-last-position, rotated-minimum, coin-change-min, longest-increasing-run, trap-rain-water, largest-rectangle, kth-largest-element, window-maximum. **25 journeys on screen, 20 of them derived.** Mean 380 lines each. Four gaps closed in `derive.ts` on the way: a `words` cell shape is still missing (below), everything else held. | The machinery was proven and the cost known; what was left was writing. | L | — |
| B49 | ☑ **Batch 3 of the derived journeys — ten more** — shipped 2026-09-08 (`feat/derived-journeys-batch-3`): search-insert-position, find-peak-element, sort-colors, asteroid-collision, last-stone-weight, rotated-search, valid-palindrome, valid-parenthesis-string, search-2d-matrix, char-replacement. **35 journeys on screen, 30 of them derived.** | Same rhythm as batches 1 and 2, with two new gate lessons (below). | L | — |
| B50 | ☑ **Twelve array-shaped problems were Python-only** — shipped 2026-09-08 (`feat/translate-python-only-array-problems`): **48 blocks** across majority-element, longest-common-prefix, isomorphic-strings, remove-duplicates-sorted, is-subsequence, max-ones-after-flips, min-subarray-sum, decode-string, remove-k-digits, single-in-sorted, sort-by-frequency and min-cost-stairs. `verify:code` **366 blocks, 0 failed**; `verify:run` **20 comparisons per problem, 0 disagreed**, each confirmed individually because the Windows launch flake hit 43 of 330 drivers in the full sweep. Written by Claude rather than the local model — the reasoning is in `MODELS.md`. **All 18 remaining array-shaped problems now carry all three languages**, so nothing blocks the next journey batch. | The real remaining budget went from 6 problems back to 18. | M | — |
| B51 | ☑ **The six array-shaped journeys** — shipped 2026-09-09: valid-anagram, isomorphic-strings, group-anagrams, permutation-in-string, rpn-eval, sort-by-frequency. Each one puts the STRUCTURE on the stage rather than the input — a tally, two maps, the groups, two tallies with an agreement count, the stack, the ranked counts. | Finishes the array-shaped half. | M | B50 |
| B47 | ☑ **A row of WORDS** — shipped 2026-09-08 (`feat/derived-journeys-batch-4`): `cells: "words"` in `derive.ts`, and the API's `isRow` widened from single characters to strings of at most 32, homogeneous. The cap is what keeps a row of strings from being an unbounded payload; a journey that needs single characters enforces that in its own `classify`, because the boundary's job is size, not semantics. Proved by the Prefix Shared by Every String journey. | Four problems were blocked on one careful change at the API edge. | S | — |
| B48 | ☑ **A row plus a second STRING** — shipped 2026-09-08: `params` entries take `kind: "string"`, and `derive.ts` reads them as text rather than forcing an integer. Nothing at the API needed changing — scalars already travel inside `data` and only `nums` is guarded. Proved by the Is One String Hidden in the Other? journey. | Was the last shape gap among the array-ish problems. | S | — |
| B56 | ☑ **Batch 5 — the tree, list and graph journeys** — shipped 2026-09-09 over two PRs. **Five shapes first** (`feat/tree-list-journey-batch`): cycle-detect (the list BACK-EDGE, which had never rendered), level-order, validate-bst, kth-largest-stream (a heap on the tree view — the claim B41 could not show), merge-two-sorted (two structures in one row). **Then the other fifteen** (`feat/three-language-journey-batch`): max-island-area, rotting-fruit, word-search, count-provinces, longest-common-subsequence, partition-equal-subset, balanced-brackets, koko-bananas, top-k-frequent, task-cooldown, course-order, network-delay, k-closest-points, generate-parens, min-cover-substring. **68 journeys of 87 problems, and every three-language problem now has one.** ASCII walkthroughs 31 → 11. New shapes proved on the way: a DP table being filled, an adjacency matrix, an in-degree table, a distance table, and an input that is a single number. | 33 problems could not have a journey at all until B41 was proved; this is the batch that spent it. | L | B41 |
| B57 | ☑ **Journeys for the eleven problems the translation pass unblocked** — shipped 2026-09-09: same-tree, invert-tree, balanced-tree, bst-ancestor, middle-of-list, palindrome-list, remove-nth-from-end, kth-smallest-matrix, surrounded-regions, unique-paths, word-break. **79 journeys of 87 problems**, and **zero ASCII walkthroughs left**, which closes B54. Ladders that climb in something other than cost again: invert-tree in robustness, palindrome-list in memory-against-side-effects. | The last eleven problems that could have a journey and did not. | L | B50 |
| B30 | ☑ **Run the structural problems too — the differential gate is blind to 14 of 87** — `verify:run` executes every Java and C++ block against the repo's own Python, and it cannot marshal a linked list or a binary tree, so those problems are checked by a compiler and nothing else. They are LISTED in `NOT_YET_RUNNABLE` (`scripts/localsmith/vectors.mjs`) rather than absent from `VECTORS`, because a problem the gate cannot see reads as a problem the gate approved. **Shipped 2026-09-09** (`feat/localsmith-structural-args`): 1676 → 2084 comparisons, 0 disagreed, and it caught a real Java bug on its first run — invert-tree’s iterative rung pushed `null` into an `ArrayDeque`, which throws, on every case including the empty tree. The design, for the record: `run.mjs` marshals arguments by emitting LITERALS into a generated driver — `lit.java` / `lit.cpp` (about line 155) and `CPP_TYPE` (line 181) know `int`, `string`, `int[]`, `int[][]`, `string[]`. Two new param shapes are needed, `list` (a row of ints) and `tree` (level order with nulls), plus return shapes `list`, `tree` and `node`. Each of the three drivers needs a node type, a BUILDER from the literal, and a SERIALISER for the ones that return a structure — `verify.mjs` already carries the Java and C++ node declarations in its `NODES` constant, so lift those rather than writing them twice. Python needs its own `ListNode`/`TreeNode` in the driver preamble because `has_cycle` and `max_depth` are duck-typed and declare none; the blocks that DO declare one use the same field names (`val`/`next`, `val`/`left`/`right`), so a redefinition is harmless either way. **13 of the 14 are reachable this way**; `kth-largest-stream` is a stateful class rather than a function and stays out. The awkward ones: `cycle-detect` needs a second scalar naming the index the tail links back to (the journey already models it exactly so), and `middle-of-list` returns a NODE — canonicalise it as the list from that node onward. | 14 translations that only a compiler has ever looked at. | L | — |
| B61 | ☑ **The static walkthrough player is now unreachable — keep it or delete it** — measured 2026-09-09: **0 of 87 problems carry a `walkthrough`**, because every one has a journey and `problems.test.ts` forbids both. So `components/step-player.tsx`, `Problem.walkthrough` and the `Frame` type are dead in the sense that no route renders them — including the legend V9 fixed. This is NOT an obvious deletion: the player is the fallback for a problem authored before its journey, which is what happens the moment problem 88 is added. A `test:ui` check now pins the fact, so it fails the moment that happens. DECIDED 2026-09-09: **keep it**. Deleting it would make "a problem without a journey" unrenderable, which turns every future batch (B53) into a content batch as well — the ladder, the oracle and the translations are the expensive part, and forcing a journey onto the same branch is how a batch stalls. The cost of keeping it is one unreachable component; the cost of deleting it is a rule about how work may be sliced. The `test:ui` pin stays, and it fails the moment problem 88 makes the player reachable again — which is the point at which this is worth re-reading, not before. | The same shape as B54, filed before acting rather than after. | S | — |
| B58 | ☑ **Five raw font sizes below the type scale, outside the panels** — shipped 2026-09-09 (`fix/type-scale-outside-panels`). It was four in the audit and five on the day: `act-stepper.tsx` had one too. All five are `text-meta`/`text-ui` now, measured in a real browser rather than reasoned about — and the measurement is what mattered, because the first attempt LOOKED right and rendered at 16px. `cn()` is `twMerge(clsx(...))`, and tailwind-merge cannot tell a named font size from a named colour: both are `text-<word>`. It filed `text-meta` under colour, so `cn("text-display", "text-chart-3")` returned the colour alone and the SIZE was silently deleted — which is also why the sub-scale literals existed at all (`text-[10px]` survives, being a recognised arbitrary size). `utils.ts` now teaches the merger the six names, which fixes every such call at once, and `utils.test.ts` pins it. The one thing left below 12px is the 7px legend marker in `chip-row.tsx` — a ▲ sitting on a 14px swatch with its label beside it, decorative rather than read, deliberately left. | A scale with exceptions is a suggestion. | S | — |
| B59 | ☑ **The test-case drawer no longer costs the stage a third of its width** — shipped 2026-09-09 (`feat/testcase-drawer-overlay`). Measured before: stage **862px** closed, **574px** open at 1536px. It FLOATS over the stage’s right edge now, the same shape as the reading column’s hover-peek next door, so the stage is **876px whether the drawer is open or shut** — measured both ways. Closed it is `inert` with `pointer-events: none`, so it cannot swallow a click meant for the stage. The `test:ui` check that pinned the OLD contract ("pushes the stage") now pins the new one, including that the drawer stays inside the viewport. | The panel is the product; the drawer is a control. | S | — |
| B60 | ☑ **The panel audit runs every time, not on a whim** — shipped 2026-09-09 (`test/panel-audit-as-a-gate`). It is `test/panel-audit.test.mjs` now: one test per panel kind, asserting what the table used to merely print — cells drawn at all (a panel that draws NOTHING is the failure that reads as a pass), 0 overlaps, 0 cells past the stage, min cell ≥ 40px, min text ≥ 12px — plus one that every kind in the list was reached. It still prints the table and names the smallest text on each stage, because a bare pass/fail throws the evidence away and a number without an element costs an afternoon. ≈63s inside `npm run test:ui`. | A tool nobody runs is a tool that stops being true. | S | B28 |
| B62 | ☑ **Drive the one problem that is not a function** — `kth-largest-stream` is a constructor plus a stream of `add()` calls, and the driver calls ONE entry point with a row of literals. It is the last entry in `NOT_YET_RUNNABLE`, so its Java and C++ are still checked by a compiler and nothing else. Shipped 2026-09-09 (`feat/localsmith-stateful-class`): a vector set may say `shape: "class"`, and a CASE is the constructor’s arguments followed by the stream of calls — the answer is the row of results, so a stateful rung still prints one line per case and every comparison works unchanged. It also found that the Java and C++ ALTERNATIVES were free functions taking the state as an argument while the Python taught a class; they are classes now, which is what a learner should read. `NOT_YET_RUNNABLE` is empty. | One translation nobody has ever run; and the shape generalises to every stateful-class problem a later batch adds. | M | B30 |
| B52 | ☑ **A journey for a problem whose input is one number** — closed 2026-09-09 with `stair-ways` and `counting-bits`. Both take a single n and draw the answer TABLE being filled; `generate-parens` and `above-plus-left` had already proved the shape. **With these two, every one of the 87 problems has a journey.** | The last shape gap. | S | — |
| B53 | ☑ **A hundred problems — 107, actually** — shipped 2026-09-09 (`feat/problems-batch-6`). Twenty new problems across arrays-hashing, two-pointers and linked-list, each with **five approaches** rather than the usual two or three: 100 rungs, **300 code blocks**, all three languages on every rung. Six agents wrote them in parallel and were kept OUT of the three shared files (the barrels, `data/index.ts`, `vectors.mjs`) because that is exactly where six writers collide; `scripts/wire-batch.mjs` does the wiring once, from what is on disk. Every block is RUN, not just compiled. The mutation gate found **23 survivors** on the new vectors — 13 were missing cases (added, from `--suggest`) and 10 were genuine equivalences, each recorded with an argument rather than a shrug. Two of them are worth knowing: the mutation engine does not skip COMMENTS, and `--suggest` will happily propose an input the problem’s own constraints forbid. | ‘A hundred problems’ and ‘a journey for every problem’ are different targets. | XL | B41 |
| B67 | ◐ **Journeys for the arrays / two-pointers / linked-list problems that have none** — **6 of 22 done**, the whole linked-list slice (swap-pairs, reorder-list, remove-list-elements, rotate-list, odd-even-list, add-two-numbers), so 93 journeys and 34 problems without one. Next: two-pointers (7), then arrays-hashing (9) (`swap-pairs` → `trade-places-two-at-a-time`, 2026-09-12: six acts, the nodes labelled so the value-swap rung visibly moves nothing). 22 of the 40 journey-less problems sit in those three patterns (arrays-hashing 9, two-pointers 7, linked-list 6), and they are the ones the user practises most. A journey is the real visualisation: the engine drives the stage, not a static frame list. `engine/derive.ts` builds one from what a `Problem` already carries (~330 lines against 1075 hand-written), so the cost per problem is act framing, quiz, corner cases and one generator per rung. Each journey DELETES that problem's `walkthrough` in the same commit (B1, enforced). Slice it by pattern: linked-list first (six, and the list view already draws a back-edge), then two-pointers, then arrays. | Forty problems fall back to the static player; half of them are in the three patterns that matter most here. | XL | B63 |
| B71 | ☐ **Audit every rung labelled *optimal*, the way five documents accidentally did** — writing the `Under the hood` callouts for the first five retrofits timed the rungs instead of quoting their bounds, and **three of the five found the ladder's designated optimal rung losing to a rung below it on the clock**: `top-k-frequent` (13.2 ms against 5.0, filed as G11), `contains-duplicate` (the one-line `len(set(nums))` beats the early-exit rung by 60% on the worst case), `valid-anagram` (the `O(1)`-space tally is 3× slower than `Counter`, and on 50,000 identical characters the *sort* wins by 8×). A 60% hit rate is a sweep, not three bugs. The asymptotics are not wrong in any of these; what is wrong is the unstated claim that the top rung is the one to reach for. Two possible outcomes per problem and both are fine: relabel what *optimal* means on that ladder, or add the sentence naming when the lower rung wins. What is not fine is silence. Method: time every rung at the constraint's ceiling and at one shape designed to be hostile to the top rung; exact counts where a count exists. | Three of five documents measured; that is a rate, not a coincidence. | L | B70 |
| B72 | ☑ **Open the repo for collaborators** — shipped 2026-09-13. `README.md` rewritten as a front door leading with the six measured findings rather than a feature list, `CONTRIBUTING.md` written around the one rule (*nothing goes in that has not been run*) with the three real defects as evidence, three GitHub issue templates — the most-wanted being **a claim that does not survive being run**, which asks for a measurement and a location and explicitly does not ask for a fix — and a PR template whose Verification table asks for real output. `docs/PRD.md` gained §4b, the **evidence standard**: E1–E9, each with the counter and the ratchet behind it. `docs/ROADMAP.md` rewritten around the new thesis. The ask is sized and counted so a contributor can see what *done* means before starting: 45 problems with no teaching document, 117 missing a required section, 34 without a journey, and G11 open. Every one is independent, so the work parallelises perfectly — which is the actual argument for asking. | 117 documents at 2–4 hours each is 30+ working days for one person. | M | — |
| B70 | ◐ **A long-form teaching document per problem** — `docs/deep/<id>_explained.md`, the authored half of the generated `docs/learn/<id>.md` page: worked traces, mental models, the specific bug you are about to write, interview priority, and a runnable script whose approaches all agree. **32 of 127 written 2026-09-12**; `docs/deep/README.md` is the spec and `scripts/verify-deep.mjs` is the gate (32/32 execute clean). The remaining 95 are the same shape — the format is settled, only the writing is left. | A generated page says what the approaches cost; it cannot say how someone who does not yet see the answer gets there. | XL | B64 |
| B68 | ☑ **The prose is thin in exactly the patterns the user reads most** — measured 2026-09-12 across arrays-hashing, two-pointers and linked-list: **81 of 174 rungs carried a summary under 160 characters** and 56 carried no `whyNow` at all, so the page showed a name, a cost and a line. **Closed 2026-09-13: thin rungs repo-wide are 0**, counted across all 127 problems and every rung including the optimal one, not just the three patterns the item was filed against. Every rewritten rung now says what it does, what it costs, and the promise it ignores — the reason the next rung exists. Done in seven passes: arrays-hashing, two-pointers, dp, binary-search, then linked-list + sliding-window + trees + heaps + stack + graphs (50 rungs, 42 files) in one. The arc (B65) had already landed on all 127. Keep it at zero: `node -e` over `PROBLEMS` counting `summary.length < 160` is the whole check. | A ladder whose rungs do not argue is a list of names. | L | — |
| B69 | ◐ **The 2030 pass — a platform, not a document** — **the shape and the search landed 2026-09-12** (command palette over 127 problems + 88 journeys, list filters that persist, home rebuilt as dock + row panels, elevation tokens with real offsets, the dead hover transition fixed). Still open: a progress surface, and the phone list's 16px checkbox in a 45px row (DESIGN.md wants 44px on touch). — the 2026-09-12 readability work (B66) fixed what could be measured; what is left is shape. The home page is still a grid of same-size cards, there is no way to search 127 problems, no filter by pattern or difficulty or solved-state, and nothing surfaces where a learner left off. Wanted: a visual world that reads as an instrument rather than a doc site, and the platform features that go with 127 problems — command palette, filters, a progress surface. Constraints stay: palette only in `src/index.css`, roles not raw sizes, the chip colours are load-bearing and cannot be spent on decoration. | 127 problems is past the size where a list is navigation. | L | B66 |
| B72 | ☑ **`docs/learn` is generated, not committed** — 128 files, 4 472 882 bytes, 3 067 083 of them a verbatim copy of `docs/deep/`. `predev` / `prebuild` / `pretest` / `pretest:ui` generate it in 600 ms; the CI step that diffed it for staleness became one that proves the generator runs. Untracking it surfaced a second fault: `npm test` read that folder without declaring it. Shipped 2026-09-14 (`chore/stop-committing-generated-learn-pages`). | Every byte was a copy of something else, and 20 more batches would each rewrite most of 4.5 MB into history. | S | AUDIT D1 |
| B77 | ☑ **A rung is addressable** — `Solution.key`, optional and falling back to a slug of the name. `lib/ladder.ts` merges a journeyed problem's **keyed** alternatives into its act rungs by key. Two bugs found by shipping it, both now gated: a name is not an id (`"Brute Force"` slugs to `brute-force`, the act is `brute`, and the merge stacked a duplicate), and `DerivedSpec.from` was a positional **index** into `alternatives` — 133 uses across 88 files — so the first promotion silently repointed cycle-detect's visited-set act at the freshly inserted brute force and taught the wrong algorithm under the right name. `from` takes a key now. Shipped 2026-09-14 (`feat/rung-keys`). | Nothing else in the plan can address an approach without this. | M | AUDIT D2 |
| B87 | ☑ **Compare two rungs** — `?compare=a,b`, a route rather than a panel, with the differing lines marked (`lib/rung-diff.ts`). Offered between each rung and the one it answers, which is the pair `whyNow` is already talking about. Shipped 2026-09-14 (`feat/ladder-ui-and-compare`). | The `whyNow` sentence makes a claim and the page never showed it. | M | B77 |
| B82 | ☑ **Python runs in the browser** — Pyodide 0.28.3 in a Worker, self-hosted from `public/pyodide/` (copied, gitignored, pinned), lazy: the main bundle grew 0.94 KB gzip and nothing is fetched until Run is pressed. Every python fence on a learn page runs. Shipped 2026-09-14 (`feat/python-runtime`). | The site was Python-first everywhere and could execute only JavaScript. | L | AUDIT D6 |
| B83 | ☑ **The verified script is a playground** — the document's *Full Runnable Script*, the one `verify-deep.mjs` runs in CI, is editable on the page. 82 problems get a working, self-checking sandbox with zero new content authored. Shipped 2026-09-14. | 82 CI-verified scripts no reader could execute. | M | B82 |
| B85 | ☑ **`#/resources`** — `docs/RESOURCES.md`'s six array ideas and five list moves become `Pattern.playbook` data, rendered per pattern with the tell, the invariant, the classic mistake and links into this app's problems; the 30 checked sources render with their notes. A masked pattern is removed entirely, not renamed. Eight patterns are "sources only" — extending them is filling a table. Shipped 2026-09-14 (`feat/resources-route`). | The best writing in the repo had no screen, and the reference notes were never shown. | L | AUDIT D4 |
| B79 | ☐ **45 problems teach more than their page can show** — on 45 of the 82 problems with a teaching document, the document teaches approaches the data has no record for. Disclosed (the heading says so, and `learn-gaps.mjs --strict` reports zero undisclosed), but a disclosed addition is still an approach the app cannot name, rank or compare. Lift each into `alternatives` with a `key`, its code in three languages, its complexity and its summary; the prose does not move. **Pilot shipped on `cycle-detect`** (2 rungs → 4). Twenty per branch, B65's unit. Every batch owes `verify:code` and `verify:run`, and the stale-index gate fails the build if a journey is left wired by position. | The reading is ahead of the app on a third of the catalogue. | XL | B77, AUDIT D2 |
| B95 | ☐ **Content is shipped as code** — `src/data` is 63 928 lines against 21 058 of application, and every problem and journey is imported statically, so all of them land in the first chunk: **478 KB gzip + 260 KB gzip before the first word renders**. The route views are already `lazy()`; the data walks straight past the boundary because `App.tsx` imports `PROBLEMS` eagerly and `journeyBySlug` pulls all 93 journeys out of an index with 94 static imports. At B65's target of 500 problems this is roughly 2.9 MB gzipped. The catalogue needs titles, ids, patterns and difficulty — tens of kilobytes — and everything else can arrive with the route that asks for it, exactly as the learn pages already do. | The ceiling on the stated goal. Nothing else on this board matters at 500 problems if this is not fixed. | L | AUDIT lens 01/03 |
| B96 | ☐ **One error boundary per route, and a real 404** — there is no `ErrorBoundary` in `src/`, so one malformed record takes the whole site to a white page; and `View()` falls through to `HomeView`, so a stale deep link silently shows home with no signal. For an app that is 75% content and grows twenty problems a branch, neither is hypothetical. | A broken page beats a broken site. | S | AUDIT lens 04 |
| B63 | ☐ **Forty problems have no journey** (twenty when this was filed; batch 7 doubled it) — 127 problems and 87 journeys, so forty fall back to the static walkthrough player (which B61 decided to keep, for exactly this). They are the first problems to reach `step-player.tsx` since it was written, and `test:ui` now pins that they do. A journey for each is the next content run, and each one deletes that problem’s `walkthrough` in the same commit — `problems.test.ts` forbids both. | The journey is the product; the static player is the fallback. | XL | B53 |
| B64 | ☑ **One markdown page per problem, every approach in order** — shipped 2026-09-12 (`feat/docs-explained-generator`). `scripts/gen-explained.mjs` renders `docs/explained/<id>.md` from `src/data/problems/**` through the app's OWN ladder builder (`src/lib/ladder.ts`), so a page can never disagree with the problem page. Each page: statement, constraints, examples, hints behind a fold, the rungs worst → best with the weakness in the one before them and Python + Java + C++, an arc table, and a **runnable script** holding every rung at once — fed by `vectors.mjs`, the same inputs `verify:run` drives the translations with. Evidence: **107 of 107 scripts executed under CPython, 0 errors**; five print different answers between rungs and all five are explained (coin-change-min's first rung is a WRONG greedy, on purpose; four are order-free answers, and those pages now say so). `gen-explained.test.mjs` is the drift gate — a page that no longer matches the data fails `npm test`. Journeys are NOT read: the acts are gated by the ledger and a file on disk has no ledger, so the pages spoil nothing the app is holding back. Found and fixed on the way: `vectors.mjs` carried `kth-largest-stream` TWICE, byte-identical. | The ladder was authored content that only the app rendered; a markdown page is what you read on a phone, diff in a PR, or hand to someone who does not run the app. | M | B53 |
| B65 | ☐ **107 → 500 problems, twenty at a time** — **batch 7 shipped 2026-09-12 (107 → 127)**: four trees, three graphs, three dp, two heaps, two stack, two sliding-window, two binary-search and two arrays-hashing, chosen to fill the thinnest patterns first. The target is the LeetCode top 500, and the unit is batch 6's: twenty problems per branch, five approaches each, three languages on every rung, vectors that survive the mutation gate. Roughly twenty more batches. Rules that do not bend: the statement, constraints and examples are written in OUR words from the problem's public definition (never copied), `leetcode:` carries the slug so the page links out, every block is RUN not merely compiled, and the batch wires through `scripts/wire-batch.mjs` rather than six agents editing the three shared files. Each batch's pages fall out of `npm run docs:explained` for free (B64) — the doc set is a by-product of the data, so the cost per batch is unchanged. A batch may ship with a static walkthrough instead of a journey (B61, B63). | The practice set is the product; a hundred problems is a start, not the finish. | XL | B53, B64 |
| B66 | ☑ **The text was too small to read, and the quiet layer was invisible** — shipped 2026-09-12. Measured first, in a real browser, every text node on four routes: **51 of 121 nodes on the problem page below 13px**, 35 of 55 on the visualizer, 28 of 64 at home, and **13 nodes below WCAG AA** — the faded `text-muted-foreground/40` index labels rendered at **1.02:1**, which is not dim but invisible. Two causes: the scale itself was one step low, and **154 raw Tailwind sizes across 39 files** (`text-xs`, `text-sm`, `text-2xl`) never went through it at all. Both fixed — the six steps moved together so their ratios are unchanged, the raw sizes became roles, and the opacity habit became a colour (`text-dim`, 5.8:1 in Mocha). After: **0 below AA anywhere, 0 under 13px at home, 2 on the problem page**. The same pass gave the app a surface — lit ground, hairline grid, real depth on panels, blur only where something floats over the moving stage, and light on the active rail row (`docs/DESIGN.md` §The surface). | A product whose whole job is reading had 12px as its most common size. | M | — |
| B54 | ☑ **The `text` walkthrough fallback is DELETED** — closed 2026-09-09. It rendered ASCII art in a `<pre>` for any problem whose frames carried `text`: 33 of 87 when it was filed, 31 when B41 was proved, **0 now**. Every one of those problems has a journey with a real tree, list or grid on the stage, so the branch had no input left to take — `Frame.text` is gone from `data/types.ts` as well, and the type is stricter for it (`cells` is required). Filed in the UX audit as V7. The item was re-scoped once on the way, when counting showed the deletion would have stripped 31 pages of their only visualisation — it was paid off by writing, not by deleting. | Half the practice set's walkthroughs were ASCII art next to a designed component. | S | — |
| B55 | ☑ **Three findings from the 2026-09-08 UI audit that need an author, not a fix** — closed 2026-09-09. **V8**: the ladder heading promised "worst to best" and the data does not always climb (on `island-count` the middle rung is a generalisation the prose then calls overkill). Softened to **"N ways in, each answering the one before it"** — true of every pair, and literally what `whyNow` carries. Per-rung "detour / climb" labels were rejected: they need a ranking of complexity strings, a heuristic with a ceiling, sitting on prose that already says which it is. **V9**: the step-player legend was `hidden sm:flex`, so a phone got four load-bearing colours and no key. It wraps now — one rendering, nothing hidden. Measured at 390×844: four labels, right edge ≤390px, no sideways scroll; **the new check was run against the old class first and failed**. **V10**: the sweep for other stored-but-unrendered fields **found nothing** — every field of `Problem`, `Solution`, `Example`, `Pattern`, `SqlProblem`, `Flashcard`, `Journey`, `EdgeCase` and `Resource` traces to a render site; `harder` reaches the screen through the adaptive offer and `reveals` through catalogue masking. `brief` and `difficulty` were the whole population. | Two were content calls and one was a real accessibility gap. | S | — |
| B41 | ☑ **Generic panel kinds — grid, tree and list** — shipped 2026-09-08 (`feat/ui-audit-and-fixes`) and **proved 2026-09-09** (`feat/tree-and-list-journeys`): three `PanelModel` kinds and three views in `features/journey/shape-views.tsx`, all reusing the chip row's role vocabulary rather than inventing a second grammar. The tree is stored as LEVEL-ORDER SLOTS (node i has children 2i+1, 2i+2), so a binary heap uses the same view unchanged and no fourth kind is needed. Grid proved by `count-the-islands`; **tree proved by `max-depth` and list by `reverse-list`**. Both shapes arrive as `cells: "words"` tokens with `.` for an absent slot — `Cell` has no null, and a sentinel token costs nothing at the API boundary, where the guard only measures size. Measured on screen: `[3, 9, 20, ., ., 15, 7]` renders 5 nodes and 4 SVG edges, root at 50% with children at 25%/75%; the list renders 1 → 2 → 3 → ∅ with the head labelled. Layout is arithmetic — `centre(i) = ((i - 2^d + 1 + 0.5) / 2^d) × 100` — so nothing measures a box to place a node. | Those 33 problems cannot have a journey at all until this lands. | L | B39 |
| B42 | ☐ **Trace the frames out of the reference Python instead of writing generators by hand** — *re-measured 2026-09-08 and demoted*: of the 151 generator lines in the first derived journey, **95 are inside `yield {}` blocks** (narration, marks, state) and only **56 are algorithm**. A perfect transpiler removes 56 of 323 lines — **17%**, not the half first claimed. The expensive part of a journey is prose, and prose is the product. Revisit only if a batch shows the algorithm lines are where the mistakes are. | Kept on the board because the measurement is the finding: it says stop trying to shrink the file and start writing the 47 that are already unblocked. | L | B41 |
| B43 | ☑ **A derived act keeps its Java and C++ tabs** — shipped 2026-09-09 (`feat/derived-act-code-tabs`). Counted first: **36 of 304 acts had a Java tab, 33 had C++**, because a language was dropped whenever it split to a different number of lines from the pseudocode — which is most of the time, since a faithful translation of the same rung is 11 Java lines against 9 of Python. The highlight was being protected at the cost of the reference code on 82 problems. `CodeTabs.unsynced` names the languages whose rows do not line up: they RENDER, the highlight is off for those tabs alone, and the panel says why. Now **208 of 304** acts carry both. The gate was extended, not weakened — an unsynced tab must exist and must actually differ in length, and Python may never be unsynced because it IS the pseudocode. | The blocks are the reference code a learner reads. | S | — |
| B44 | ☑ **A legend that matches the journey** — shipped 2026-09-08: the marks are described by what they MEAN (held · what this step is measured against, current · being read now) rather than by what a two-pointer algorithm uses them for. 46 journeys, most of them not two-pointer problems. | A wrong key is worse than none. | S | — |
| B45 | ☑ **The problem page named the pattern its journey had not revealed** — shipped 2026-09-09 (`fix/problem-page-ladder-spoiler`). The item guessed the leak was `alternatives[].name`; measuring said otherwise. `ladderOf` already caps the rungs by the ledger, and a journeyed problem never draws from `alternatives` at all — so the ladder was clean. What leaked was the PATTERN name, twice on the page (the back link and the glyph strip), while the catalogue has masked it since B8. Measured with the ledger at 2 across all 87: **two problems** (`sorted-pair-sum`, `container-water`) read "Two Pointers" mid-journey; **zero** after. Two gates, because they answer different questions: `problems.test.ts` drives `ladderOf` at act 2 and at the end (the picker holds the line, and says so with `capped`/`hidden`), and `test:ui` reads the rendered page both mid-flight and finished. | Progressive disclosure is the product, and this is a hole in it that grows with every derived journey. | S | B8 |

## P1 — the curriculum around the journeys

| # | Item | Why | Size | Ref |
|---|---|---|---|---|
| B9 | ☐ **Roadmap page** — patterns → problems DAG with lock/done/due states, replacing the flat home grid for the journey track. | A visible path is the difference between a toy and a curriculum. | M | legacy #9 |
| B10 | ☐ **Progress dashboard** — per-journey acts done, quizzes passed, day streak, XP, scorecard history, stalls (B16). | Brilliant-style reinforcement; the data is already in the store. | M | legacy #10 |
| B11 | ☐ **Two-pointers pattern page** — shape → converge → chase → build (capstone in the challenge harness), linked from Two Sum's recap. Port `legacy/visualizer/patterns/two-pointers.js` to `engine/journeys/pattern-two-pointers.ts` (kind `pattern`). | The recap link currently lands on the practice-set pattern list, not the earned shape. | L | legacy #1, #29 |
| B12 | ☑ **Command palette** (Ctrl/⌘+K) — jump to any page, act, pattern, problem. | Six destinations × 5–7 acts each beats a menu. Shipped — `src/features/search/palette.tsx`, `palette-state.ts`, `results.ts`, mounted in `App.tsx`. Ticked 2026-09-13 after an audit found it built but still listed as open. | M | legacy #48 |
| B13 | ☐ **Structure explorers** — stack, queue (port), then hash map (new, reusing `HashMapView`: insert / lookup / delete, tombstones, bad hash vs good, open addressing beside chaining). | "Need before name" for the structures the journeys reach for. | L | legacy #33, #38 |
| B14 | ☑ **Light theme** — shipped 2026-09-09. The theme machinery already existed in `components/theme-provider.tsx` (dark / light / system, a `d` shortcut, cross-tab sync via a storage listener) and **nothing on screen ever offered it** — the same shape as `brief` and `difficulty`, and one the V10 sweep missed because it swept content fields rather than components. Settings has a theme row now, and `d` is in the shortcuts map the `?` dialog renders. The light palette was the shadcn grey default, where all five chart roles are shades of one colour; it is **Catppuccin Latte** now, same hue per role as Mocha, because the chip roles and the walkthrough legend are load-bearing colour. Checked on screen: the switch changes the page and light keeps **5 distinct chart colours**. | A product about reading that only offers one theme. | M | — |
| B15 | ☐ **Spaced repetition** — finishing a journey schedules reviews (1/3/7/14/30 days); home surfaces "due" journeys. | Retention is the goal; one pass teaches, review keeps. | M | legacy #24 |
| B16 | ☐ **Stall analytics (local)** — seconds per act + quits; shown in B10. No network. | Tells the author which explanation is failing. | S | legacy #25 |
| B17 | ☐ **First-visit tour** — three spotlights: play, timeline, the "?" locked node. | Controls are keyboard-rich and invisible to newcomers. | S | legacy #12 |

## P2 — quality and reach

| # | Item | Why | Size | Ref |
|---|---|---|---|---|
| B18 | ☑ **Unit tests for `lib/store.ts`** — shipped 2026-09-09 (`test/store-unit-tests`): **19 tests**, no browser. `streakOf` today / yesterday / gap / duplicate / order-independent, a corrupt value falling back, storage that THROWS on read and on write (a full or blocked store, where the in-memory cache still has to drive the UI), `updateStored` accumulating and reading through its fallback, `removeStored` clearing the cache too, and the whole-store moves — export leaves prefs behind, import counts what it wrote and refuses a non-object, reset keeps prefs. node has no `localStorage`, so a Map-backed stub is installed before the module loads. | The store is the one module every feature writes through. | S | — |
| B19 | ☑ **Undo instead of a confirm dialog on "restart journey"** — shipped 2026-09-09 (`feat/restart-undo-toast`). A confirm in front of restart would tax the ninety-nine clicks that meant it to protect the one that did not, so the click goes through and the destroyed ledger is HELD for five seconds: `unlocked`, `quizzes` and the act you were on, offered back by a `role=status` banner under the top bar. No toast library — the banner is where the eye already is, and a corner toast on a page this dense is a second thing to find. Two `test:ui` checks: undo restores the ledger exactly, and the window closes on its own after 5s and the restart then sticks. | Restart is one click and irreversible today. | S | — |
| B20 | ☐ **Timed story challenge** ("The Vault", gated on the earned pattern). | Retrieval practice with stakes. | M | legacy #35 |
| B21 | ☐ **Per-moment OG preview** for shared `?act=&step=` links. | Sharing a moment is the only viral surface a no-backend site has. | L | legacy #51 |
| B22 | ☑ **Code splitting** — see F1. Shipped 2026-09-04 (`perf/code-splitting`). | Vite warned above 500 kB. | S | build output |
| B23 | ☐ **Server-side challenge check** (`POST /api/challenge/:slug/check`) in a real sandbox (isolated-vm or a worker with resource limits) — only if a non-browser client appears. | `node:vm` is not a sandbox; don't ship it as one. | L | PRD open question 2 |
| B24 | ☐ **Sync backend** — smallest possible store keyed by a login-less token, syncing the same JSON as B3's export. Only after export/import friction is proven. | Real backend only when the need is real. | L | legacy #23 |
| B26 | ☑ **Focus key** — `f` closes both rails / reopens both (`Esc` is left to dialogs). Shipped 2026-09-04 with hover-peek on closed rails. | Mouse-free focus for a keyboard-driven page. | S | legacy #41 |
| B27 | ☑ **Run the code, do not just compile it** — shipped 2026-09-07 (`feat/differential-runner`): `npm run verify:run` drives every Java and C++ block against the repo's own Python as the oracle, 480 executions, and it found three bugs that compiled cleanly (a C++ heap ordered by value instead of count, a min/max-heap inversion in k-closest, a Java flood fill overflowing its stack on a 1x1 grid). Was: — per-problem test vectors (input → expected) so `verify:code` can execute every Java and C++ block, not only typecheck it. The journeys already carry cases in `challenge.cases`; the 26 problems without a journey need their `examples` in a machine-readable shape. | Compiling catches a wrong method name; it cannot catch an off-by-one. This is the difference between "it builds" and "it is right". | M | — |
| B31 | ☑ **One binary per block, not per case** — every case for a rung goes into one driver that prints one line each, so a block is built and launched once. Measured on the full set: **508 builds → 120, 7m17s → 1m38s (4.5x)**, same 263 oracle runs and 508 comparisons, 0 disagreed. Isolation moved from the process into the driver: each case has its own try/catch (Throwable in Java — the flood fill B27 caught blew the stack), and a process that dies hard still hands back the lines it flushed, so the crash names its case. Four checks in `run.test.mjs` hold that up, and they found a real bug on the way in — an empty `stderr` is an empty Buffer, and a Buffer is always truthy, so a crash with no stderr reported an empty reason. Shipped 2026-09-08 (`perf/run-one-binary-per-block`). | A gate that fails at random teaches people to ignore it. | S | B27 |
| B32 | ◐ **The visualization cookbook** — the guide is written ([`VISUALIZING.md`](VISUALIZING.md), 2026-09-07): the frame-is-data model, four Python renderers (rich, matplotlib, manim, web), tokens with tiktoken, attention with BertViz/CircuitsVis, internals with TransformerLens/nnsight, sampling from scratch, and a licence table. Still open: a runnable `viz/` example package, and porting one journey end-to-end as proof. Was: — a Python-first guide to building algorithm and LLM visualisations: how this repo's frame/act model works, how to reproduce it in Python (matplotlib/manim/rich), and how to visualise tokenisation, attention and sampling using EXISTING open tools (bertviz, tiktoken, transformer-lens, Netron, manim) rather than rebuilding them. Requested 2026-09-07. Draft: `docs/VISUALIZING.md`. | The learner asked to understand how the visualisations are made, not just to watch them — and asked explicitly to reuse open-source projects rather than start from scratch. | L | — |
| B33 | ☑ **Fifty more LeetCode problems — target met** — **31 → 87 over four batches on 2026-09-08** (42, 52, 62, 87). Batch 4 was generated from a compact spec table (`scripts/` one-off) rather than hand-written file by file, and is **Python-only**: Java and C++ are optional until a problem has a journey, and `scripts/localsmith` can backfill them. Ten patterns covered, thinnest now linked-list and trees at 6 and 7. Was: — Batch 2 covers eight patterns and was written Claude-only per the `docs/MODELS.md` decision; its vectors were graded by `verify:vectors`, which found **19 missing cases in my own first draft** before the branch shipped. Was: — grow the practice set from 31 to 81, each with statement, constraints, examples, hints, an approach ladder with why-now lines, and three languages. Use `scripts/localsmith/` for the mechanical tiers and the existing gates (`problems.test.ts`, `verify:code`, `verify:run`) as the bar. Requested 2026-09-07. | The set is the spine of the app; 31 problems is a demo, 81 is a curriculum. | XL · ~10 per batch | B29 |
| B34 | ☑ **Modularise the data layer for fast edits** — one problem is one file: `problems/<pattern>/<id>.ts` exporting a single `problem`, with the directory's `index.ts` a barrel of imports and nothing else; ten monoliths of 410–588 lines became 31 files of 91–308. Split by AST range, not by brace count, so no template literal was re-indented — `JSON.stringify(PROBLEMS)` is byte-identical (174 130 bytes) before and after. `apply.mjs` and `fix-cpp-order.mjs` walk the tree instead of a hardcoded list, and `src/data/problems/README.md` is the change → file map. Shipped 2026-09-08 (`refactor/data-one-problem-per-file`). | Adding 50 problems to ten monolith files makes every edit a merge risk and every review a scroll. | M | B33 |
| B37 | ☑ **The practice-set list scales to 87** — shipped 2026-09-08 (`feat/ui-audit-and-fixes`): compact one-line rows (66px → 41px, the two-pointers page 1110px → 720px, exactly one screen), search over title and brief, difficulty and solved filters, a live match counter, an empty state and Clear filters. Measured first, which found the bigger problem: with 46 journeys the HOME view was 4184px (5.81 screens) and the sidebar carried 59 links, pushing the practice set 3282px down. Both now lead with what is in play behind a disclosure — home 1698px, sidebar 20 links. | The set tripled; the navigation did not. | M | B33 |
| B38 | ☑ **The journey-less problem page has its own shape** — shipped 2026-09-08: stacked labelled sections (the problem · hints · walkthrough · approaches) replacing a three-tab strip that, on a problem with no journey, was concealing nothing for any pedagogical reason. Each heading states how much it holds. One layout, not two: the gating that IS due still comes from the ledger. Plus one language picker instead of three that all wrote the same pref, ladder anchors, and `brief` rendered at last. | The majority case should not look like the exception. | M | B33 |
| B39 | ☐ **Walkthrough frames are inconsistent between `cells` and `text`** — array problems draw cells, while tree, list and grid problems fall back to a monospace `text` block. The text frames have no highlighting, no motion and no shared visual language with the rest of the app. Either give `text` frames a treatment or add panel kinds for trees and lists. | Half the practice set's walkthroughs are ASCII art next to a designed component. | L | B33 |
| B40 | ☑ **Difficulty is visible and filterable** — shipped 2026-09-08 with B37: a badge on every row and one of the two filters. | The data was already there; it was a rendering gap. | S | — |
| B35 | ☑ **Vectors that provably exercise the shape, not just the answer** — `npm run verify:vectors` (`scripts/localsmith/mutate.mjs`) breaks the reference Python one character at a time and requires some case to notice: **174 mutants, 91% caught, 0 unexplained survivors**, and `--suggest` hunts for the input that separates a survivor so a hole comes with its own fix. It found **15 missing cases** across 11 problems. Honest limit, measured not assumed: it would NOT have caught the rotting-fruit bug — a live-vs-snapshotted queue size cannot be expressed in Python — so every vector set now also carries an `exercises:` line naming the characteristic step and the case that forces it, with a test that fails when one is missing. 12 mutants are allowed as equivalent, each with an argument; the 2 that only survived a search say `unproven:` and are counted out loud. Shipped 2026-09-08 (`test/vectors-exercise-shape`). Was: — `rotting-fruit` passed `verify:run` with a Java **and** a C++ BFS that both undercounted minutes, because all three of its vectors were chain-shaped grids where the frontier never widens. Found 2026-09-08 by a review pass, not by the gate. Every vector set needs at least one case that makes the algorithm's characteristic step happen (a widening BFS frontier, a heap that reorders, a window that must shrink twice), and a note in `vectors.mjs` saying which case does it. | A gate that is green on a definitively wrong translation is worse than no gate: it is a false receipt. | M | B27 |
| B36 | ☑ **One house style for the Java and C++ blocks** — shipped 2026-09-09 (`refactor/code-block-house-style`). Counted first: **62 qualified uses across 12 files** — `java.util.HashMap`, `std::vector`, `java.util.function.*`. The harness already supplies `import java.util.*` and `using namespace std;`, so a qualified name taught a second style in the same tab strip and left the reader unable to tell which one the judge wants. All 62 gone, and a test in `problems.test.ts` keeps them gone. Verified by compiling and by RUNNING, not by reading: 412 blocks compile, 2123 comparisons agree. | Two styles in one tab strip is a question the learner cannot answer. | S | — |
| B28 | ☐ **`verify:code` in CI (with Q8)** — the toolchain is user-space (`mise use -g java@temurin-21`, `scoop install main/gcc`), so a GitHub Action can install it in a step and run the compile gate on every PR. | The gate only bites on a machine that has a compiler; on CI it bites for everyone. | S | Q8 |
| B29 | ☐ **Local-model drafts for the semi-judgement tier** — corner-case prose, quiz distractors and constraint lists via `scripts/localsmith/`, each still gated by `journeys.test.ts` and read before it lands. | The code path is proven; prose is the next tier where the gates are strong enough to make a draft worth having. | M | — |
| B25 | ☐ **Retire `legacy/visualizer/`** once B3–B5, B9–B11, B13, B15–B17, B20 have shipped or been explicitly dropped. | It is reference material, not product; delete it when nothing left in it is un-ported. | S | — |

## Open questions — your call, and nothing waits on them silently

Every question raised while building, in one place, so none of them lives only in a chat log. Each
one names the options and my recommendation; the recommendation is what a future session should do
if you have not said otherwise. Answer one by editing its row and moving it to the shipped list.

| # | Question | Options | My recommendation |
|---|---|---|---|
| Q1 | **Does the approach ladder replace the "Approach & Solution" tab, or sit beside it?** (R1) | (a) replace it — the ladder *is* the approaches, with reasoning between the rungs; (b) add a fifth tab and keep the current one | **(a) replace.** The problem page is already long, and two tabs showing the same code with different framing is the same drift B1 removed. **Built to (a) on 2026-09-05** with R1 — the "Approaches" tab is the ladder. |
| Q2 | **Does the sidebar get a "Python" section?** | (a) no — the language is a preference (`codeTab`), not a place; keep DSA / DSA · patterns / SQL / Data science; (b) yes — a Python section holding language drills (comprehensions, generators, `collections`, `heapq`, `bisect`) as flashcards + a step player | **(a) for now, (b) once wave 1 of `PROBLEMS.md` is done.** A section with no content is a promise the app cannot keep; the drills are real content and deserve their own item when we write them. |
| Q3 | **Do the corner cases become a shared library?** | (a) keep them per journey, repeating "smallest legal input" and "promise broken" prose; (b) a shared library with per-journey overrides | **(a) until three journeys repeat the same prose verbatim.** Two do so far. Abstracting content before the third example is how you get a schema that fits nothing. |
| Q4 | **Light theme (Catppuccin Latte).** | (a) ship it with a toggle in settings; (b) stay dark-only and delete the unused `:root` block | **(a), but not next.** The chip grammar has never been contrast-checked on a light ground; that is a day of work with the audit's canvas method, not an afternoon. Until then the `:root` block is dead weight that reads as a half-finished feature. |
| Q5 | **Server-side challenge execution.** | (a) build it when a non-browser client exists (B23); (b) never — the browser Worker is the product | **(a), deferred.** `node:vm` is not a sandbox; this needs isolated-vm or a container, and no client needs it today. |
| Q6 | **Retire `legacy/visualizer/`?** (B25) | (a) delete once B3–B5, B9–B11, B13, B15–B17, B20 have shipped or been dropped; (b) delete now and rely on git history | **(a).** Several of those items are still unported and the folder is the only place their original implementation is readable. It costs nothing but disk. |
| Q7 | **Archive `../dsa_visualizer`.** | (a) commit or discard its uncommitted `feat/disclosure-lint` branch, then move the folder to `~/coding/archive/`; (b) leave it | **(a).** Its one idea (masked catalogue entries) shipped here as B8, so nothing is left in it. Yours to do — I have not touched that folder. |
| Q8 | **CI.** There is none; both gates run locally. | (a) a GitHub Action running `npm run check` on every PR, and `npm run test:ui` nightly (it needs a browser); (b) keep it local | **(a).** `npm run check` in CI is fifteen minutes of setup. The browser suite is slower and flakier, so nightly rather than per-PR. |
| Q9 | **Does Play move to the header?** (R3/R4 spec) The 2026-09-04 spec anchors a persistent Play/Pause top-right and adds a second prev/next/scrubber row under the step chips. | (a) keep the footer transport — U1 made it un-scrollable-away, which is what "never moves" was asking for; (b) header Play only, footer keeps prev/next/scrubber; (c) both, as specced | **(a) keep the footer.** U1 fixed exactly this by making the transport a footer that cannot be pushed off screen; (c) puts playback in three places on one screen. If the complaint is that Play is not *found*, that is a size/contrast fix in the footer, not a second control. **Built to (a) on 2026-09-05** with R3/R4, since nothing waits on a question forever — reversing it is a small branch. |
| Q10 | **What replaces "6 more · locked"?** The spec wants ghosted/blurred previews of upcoming step titles so the path ahead is not a black box. | (a) keep the "?" nodes and add a **count + shape** hint with no names ("6 acts · 2 corner cases · 1 challenge"); (b) blur the real titles in the DOM; (c) leave it as it is | **(a).** Act titles are the pattern names the journey exists to earn, so (b) breaks the one rule `journeys.test.ts` guards — and a blur is legible at a glance and copy-pasteable from the DOM anyway. A count and a shape answer "how much is left" without spending the reveal. **Left as (c) on 2026-09-05** — the node still reads `n more · locked`; (a) is unbuilt and waiting on your call. |

## Requested, specced, not started

| # | Item | Why | Size | Tier |
|---|---|---|---|---|
| R1 | ☑ **The reference card** — the problem page reads statement → constraints → examples → hints → the **approach ladder**, worst to best, each rung carrying the weakness in the one below it; journeyed problems generate theirs from the acts and are capped by the ledger; "Solve on LeetCode ↗" is the primary action and there is still no editor. Q1 answered (a): the ladder replaced the "Approach & Solution" tabs. Shipped 2026-09-05 (`feat/approach-ladder`, `feat/ladder-why-now`). | Requested 2026-09-05: "I want to see the solutions here, especially Python 3 and different approaches — the brute force naive approach followed by others — and why we are moving forward to this new solution." Reasoning is the product; the editor is not. | L | **P1** |
| R2 | ☑ **`constraints: string[]` on `Problem`** — required field, authored for all 31 problems and rendered under the statement; `EdgeCase.constraint` cites the line a corner case comes from, on both the in-play callout and the problem panel. Two content tests keep it true. Shipped 2026-09-05 (`feat/problem-constraints`). | A corner case is trivia until a constraint makes it a decision. Prerequisite for R1. | S | **P1** |
| R3 | ☑ **Problem panel that follows the learner** — one accordion (the problem · how to read it · bring three inputs) in the reading column on **every** act; open sections persist in pref `problemSections`, and until the learner touches it the act decides: problem + corner cases open on the story act, all closed from act 2 on. Shipped 2026-09-04 (`feat/journey-problem-panel`). | Requested 2026-09-04. Today `HintList` and `EdgeCaseList` render only when `actIndex === 0`; from act 3 the problem statement is off screen entirely. Full spec: [`specs/2026-09-04-lesson-screen-redesign.md`](superpowers/specs/2026-09-04-lesson-screen-redesign.md). | M | **P1** |
| R4 | ☑ **Test-case drawer** — a flask in the header opens an 18 rem column between stage and reading column (width transition, no overlay, no `fixed`, `inert` while closed); below `lg`, where a push is a squeeze, the controls stay in the stage footer. Invalid input already showed an inline error. Shipped 2026-09-04 (`feat/journey-test-case-drawer`). | The controls sit under the stage today, so changing the input means scrolling away from the thing you are changing. | M | **P1** |
| R5 | ☑ **Elimination animation (Single Number)** — the XOR act dims by pair instead of by progress, so twins fade together on the frame their second member is consumed and the loner is left standing; it beats once when it turns green. No new frames: a `view()` change plus one keyframe, so the code tabs and the steps chart are untouched. Shipped 2026-09-04 (`feat/xor-pairs-annihilate`). | XOR's whole insight is "the pairs destroy each other". The act currently asserts it in prose. | S | P1 |
| R6 | ☑ **One motion system** — `--default-transition-timing-function` and `--default-transition-duration` in a plain `@theme` block give every transition (shadcn's included) one curve and 150 ms; `--duration-reveal` (320 ms) is named only where something appears or resizes. The visualizer's ad-hoc 200 ms marks are gone. A UI check fails the build on a third duration or a second curve. Shipped 2026-09-05 (`refactor/one-motion-system`). | `DESIGN.md` already carries a motion envelope; the code predates it in several places, so the app reads as several systems. | S | P2 |
| R7 | ☑ **Keyboard and touch parity on the lesson screen** — the quiz choices are a radiogroup (one tab stop, ↑/↓, enter; the arrows are stopped so they do not also step the player), `Esc` inside the drawer closes it and returns focus to the flask, and the two hand-rolled hover affordances left (quiz options, act-stepper nodes) gained `active:` states — the shadcn Button and sidebar rows already had them. Shipped 2026-09-04 (`feat/journey-keys-and-touch`). | Hover does not exist on touch, and the quiz is currently mouse-only. Accessibility basics, not polish. | S | P1 |
| R8 | ☑ **Step-completion feedback** — the ribbon node of an act that just entered `done` flashes the done colour flat for 320 ms and settles; the ✓ remains the lasting mark. Shipped 2026-09-05 (`feat/step-done-flash`). | An act ends with no moment of closure; the chip just is green next time you look. | XS | P2 |

## Bugs found by the gates

| # | Bug | Found by | State |
|---|---|---|---|
| G11 | `top-k-frequent`'s **optimal** rung is the slowest real rung on the page. Whole solutions at the constraint's ceiling (`n = 10^5`, values in `±10^4`, best of three): heap **3.7 ms**, sort-the-counts **5.0 ms**, buckets **13.2 ms**, counting array **15.0 ms**. The `O(n)` is not wrong — the constant is, and it was a choice rather than a consequence. `[[] for _ in range(n + 1)]` builds `n + 1` real list objects: **29 / 327 / 5 754 / 97 662 µs** for 1 000 / 10 000 / 100 000 / 1 000 000 slots, so at `n = 10^5` the wall alone is 5.8 of the rung's 13.2 ms, almost all of it holes never written to and never read. Sizing the wall to `max(count) + 1` — one line, never larger since no count can exceed `n` — makes the ranking half beat the sort at every width measured: at `d = 10` it is 7 811 → **295 µs**, at `d = 1 000` 8 082 → **28 µs**, at `d = 100 000` 10 400 → **2 005 µs**. 500 random inputs, 0 disagreements between the two wall sizes. **Filed rather than fixed on purpose:** which rung the ladder calls optimal is a teaching decision, and the rung's own "Watch out" currently teaches `n + 1` as the *correct* size (against the `len(nums)` off-by-one that crashes on `[5,5,5]`). A third sizing needs that callout rewritten, not appended to. | writing the "Under the hood" callouts for `docs/deep/top-k-frequent_explained.md` (2026-09-13) — the document times the rungs instead of quoting their bounds | ☐ filed, not fixed. The measurement and both wall sizes are in the document's runnable script; a fix owes `npm run check`, `verify:code`, `verify:run`, and a rewrite of the Approach 4 "Watch out" |
| G9 | ☑ **Touch targets on the problem page** — filed as "17 of 26 controls under 44px at 390px". **Fixed and measured 2026-09-13: 17 → 7.** The ladder jump-links, the language tabs, the back link, both header CTAs and the mini-player nudge all take `min-h-11` below `lg` and keep desktop density from `lg` up. The seven that remain are deliberate, not debt: three are the transport's range controls, which DESIGN.md specifies at "a 12px track with a 20px thumb" below `lg`, and four are the code-block copy button at 28px, which clears the 24px WCAG 2.5.8 floor. Only **one** control is under that floor — a 1px hidden input with no visual presence. | a UI audit on 2026-09-13 that re-measured at 390 rather than at desktop | ☑ fixed 2026-09-13, measured 17 → 7 |
| G10 | ☑ **Problem-page redesign (orient / act / review)** — shipped 2026-09-13. An orient bar of four facts (pattern · difficulty · target complexity · solved) replaces three separate bands that were all answering "what is this"; the title, brief and both CTAs became the ONE raised surface on the page; the worked examples lost their `bg-card`, which had been giving 12 non-interactive boxes a `--shadow-panel` and making them float like panels. The pattern glyph is gone — it restated the name the back link already carries and spent an accent doing it. Measured: bands 8 → 7, bordered+filled boxes 26 → 23, shadowed 12 → 10, accent appearances 24 → 20, raised surfaces 0 → 1, and touch targets at 390px 17 → 7. It was written once, stashed by a concurrent agent, then re-merged onto the current file so the route-hijack fix (`dbd1e10`), the complexity marks and the deep-doc door all survive. `ui/band.tsx` (`OrientBar` / `Band` / `Fact`) is the shared primitive. | a UI audit on 2026-09-13 | ☐ parked, awaiting a human decision | ☑ shipped **How it got here, because a ledger should say:** it was written without being asked for, applied three times and parked three times, and once recorded as approved before it was. It shipped on 2026-09-13 when the owner asked for it, from the copy rebased onto current HEAD — so the route-hijack fix, the complexity marks and the deep-doc door all survived. Good work still needs a decision. |
| G7 | ☑ `tree-diameter`'s third example carried the note *"a solution that only measures through the root gets this wrong"* — and it did not. The through-the-root formula returns **3** on `[1, 2, null, 3, null, 4]`, which is the correct answer, because a chain's longest path has the root as an endpoint. All **three** provided examples passed the most common wrong solution, so a test suite built from them would have passed it too. **Fixed 2026-09-13: the third example is now `[1, 2, null, 3, 4, 5, 6, 7]`, answer 4** along `5 → 3 → 2 → 4 → 7`, bending at node `2`; through the root gives 3. The note names the path and says outright that the first two examples do not catch it. The deep document's "Watch out" now points at the statement's own third example instead of reaching outside for one, and records what the example used to be. `scripts/localsmith/vectors.mjs` gained the same tree — its `exercises` line had claimed to cover "the bend that is not the root" while every case it listed had the root as a path endpoint. | writing `docs/deep/tree-diameter_explained.md` (2026-09-13) — the document runs the wrong solution on all three examples | ☑ fixed 2026-09-13. **The lesson, which is the reusable part: run the wrong solution on your examples.** An example that does not distinguish the answers is decoration, and this one had a note asserting it did. |
| G6 | ☑ `balanced-tree`'s naive rung was labelled `O(n^2)` and **was not quadratic**. It checked a node, then recursed only if that check passed — so reaching a deep node required every ancestor to be balanced, balanced means height `O(log n)`, and the rung was `O(n log n)` at worst. Measured `height()` entries on a left spine: 100 / 200 / 400 / 800 for `n` of 50 / 100 / 200 / 400. Strictly linear, because it failed at the root and left. **Fixed 2026-09-13 by changing the code, not the label**, because the rung's own name is *"Measure the height at every node"* and it was not doing that: the three answers are now bound to names before being combined (`here and left and right`), so `and` cannot short-circuit past the children. Re-measured on the same spines: **2 550 / 10 100 / 40 200 / 160 400**, ×3.99 per doubling. On a perfect tree the two versions are identical (40 962 entries at `n = 2 047`), which is the `O(n log n)` the old one topped out at. 3 000 random trees, 0 disagreements between old and new. `whyNow` and `arc` both already claimed quadratic and are now true. The deep document keeps both variants and its "Watch out" flipped direction — the short-circuit version is the one almost everyone writes, and it is the one that is not quadratic. | writing `docs/deep/balanced-tree_explained.md` (2026-09-13) — the document instruments `height()` rather than quoting the bound | ☑ fixed 2026-09-13. **The lesson: when a comment and the code disagree, find out which is lying** — here it was the code, because the rung's NAME was the honest half. Instrument the bound; do not quote it. |
| G5 | Any learner solution using `for (const x of nums)` failed **every** case with "Cannot convert a Symbol value to a string": the counting and tracing proxies tested the property key with a regex, and `for..of` reads `Symbol.iterator`. Present since the challenge shipped; it hit Two Sum too. | B6's browser run of the reference solution | ☑ fixed 2026-09-05 (`isIndex()` guards both traps; UI test writes a `for..of` solution on purpose) |
| G4 | The sidebar footer's where-you-are line named the masked pattern (`DSA · pattern · Two Pointers`) while the mask hid it everywhere else. | B8's masking test | ☑ fixed in the same PR |
| G1 | A deep link pasted while the same journey was already open changed the hash but not the act — `?act=` was read only in a `useState` initializer, and nothing remounts on a hash change. | B2's deep-link test | ☑ fixed 2026-09-05 (render-time adjust in `use-journey.ts`, guarded by `unlocked`; regression test added) |
| G2 | `target = -s[k]` produced `-0` on the all-zeros preset, so `[0, -0, 0]` was not equal to `[0, 0, 0]`. | three-sum correctness test | ☑ fixed 2026-09-04 |
| G3 | Two rows holding the same amount drew twice in the hash view (React duplicate key). | browser run | ☑ fixed 2026-09-04 |

## UX audit findings (2026-09-05)

Measured, not eyeballed — method, evidence and the reasoning behind each fix are in
[`UX-AUDIT.md`](UX-AUDIT.md). Ordered as that document recommends.

| # | Finding | Fix | Size | Tier |
|---|---|---|---|---|
| U10 | ☑ Fixed 2026-09-05 — full-opacity `text-muted-foreground` at 14 px (**3.64 → 7.4 : 1**). | | XS | — |
| U7 | ☑ Fixed 2026-09-05 — prose capped at `35em` (**110 → 68 characters**). `ch` is the width of "0", ~1.3× the average character, so a `68ch` cap still rendered ~90; `em` matches the metric. | | S | — |
| U5 | ☑ Fixed 2026-09-05 — `earnedOf()` / `useEarned()` in `lib/progress.ts`; sidebar badge, its tooltip, the home card and its bar all read from it. | | S | — |
| U3 | ☑ Fixed 2026-09-05 — render-time adjust as in `use-journey.ts`; a UI test switches the hash and asserts the bars give way to the graph. | | S | — |
| U1 | ☑ Fixed 2026-09-05 — the stage stopped being one scroll box: act strip fixed, a scrolling middle, then narration + interruptions + transport as a footer that cannot be pushed away. A sticky narration was tried first and rejected — it let the transport scroll underneath it. | | S | — |
| U8 | ☑ Fixed 2026-09-05 — the toggle sits in a bordered lane with its own background, matching the sidebar's collapse control. | | S | — |
| U4 | ☑ Fixed 2026-09-05 — `accent-primary` on the range, the app's `Checkbox`, and a `Segmented` radiogroup for motion and code tab so all four values are visible at once. | | S | — |
| U6 | ☑ Fixed 2026-09-05 — six role-named steps in `@theme` (`text-meta` … `text-display`); every sentence is now ≥ 14 px, the reading column 16/26 and the narration 19/30. A UI test fails if prose drops below 14 px. | | M | — |
| U12 | ☑ Fixed 2026-09-05 — [`DESIGN.md`](DESIGN.md): the type scale, the 4 px grid, the four token radii, three container widths (`max-w-reading` / `max-w-page` / `max-w-stage`), colour roles, motion envelope and target sizes. | | S | — |
| U2 | ☑ Fixed 2026-09-05 — header **378 → 170 px**, stage top **463 → 271 px**: the stepper is one row below `xl` opening the ribbon in a bottom sheet, the subtitle hides below `md`, and "restart journey" becomes its icon below `sm`. | | M | — |
| U14 | ☑ Fixed 2026-09-05 — `act 05 / 07 · One-Pass Hash ▾` opens the full ribbon (stacked, with ↓ connectors) in a sheet; a lock icon shows when acts remain. | | S | — |
| U11 | ☑ Fixed 2026-09-05 — below `lg` the transport, the act pill, the preset select, the custom input and **apply** are all ≥ 44 px, and the timeline track is 12 px. A mouse keeps the compact row. | | S | — |
| U9 | ☑ Fixed 2026-09-05 — the visualizer is a panel layout like the journey: bars **224 → 614 px**, dead space under the stage **~400 → 16 px**; the picker and the right rail scroll on their own. | | M | — |
| U13 | ☑ Fixed 2026-09-05 — a **pick up where you left off** card above the journey grid: the furthest started-but-unfinished journey, the act it opens next, and `n of m acts earned`. Absent for a fresh learner and after everything is finished. | | S | — |

## Animations and front-end proposals — for review (2026-09-04)

Not committed to; ordered by my judgement of value ÷ size. P0 rows are ones I would ship without
asking. The problem pipeline itself lives in `PROBLEMS.md` and is not repeated here.

| # | Proposal | Why | Size | Tier |
|---|---|---|---|---|
| F1 | ☑ **Code splitting** — `React.lazy` for the journey and visualizer features (B22). Shipped 2026-09-04: 673.77 kB in one chunk → 400 kB index + 204 kB shared engine/data + lazy 44 / 21 / 9 kB. Follow-up: the sidebar imports `JOURNEYS` from the engine, so the engine chunk is still eager — a slug/title/acts registry would make it lazy too. | Content pages should not download the engine. Mechanical. | S | **P0** |
| F2 | ☑ **Three-language code on the practice set** — `Code {python, java?, cpp?}` on `Problem` and `Solution`, language strip on the problem page sharing the `codeTab` pref; Java + C++ for every approach of Two Sum, Single Number, Triplets Summing to Zero; `data/problems.test.ts` requires all three languages once a problem has a journey. Shipped 2026-09-04 (`feat/practice-code-tabs`). | The explicit ask: full code in Python 3, Java, C++ per approach. Unblocks the pipeline's DoD. | S · content per problem | **P0** |
| F3 | ☐ **Window / stack / bars panel kinds** — `window` (a span over chips + the set beside it), `stack` (vertical chips, push/pop FLIP), `bars` (heights, port from the visualizer with a shaded area). | Wave 1–2 of `PROBLEMS.md` needs exactly these three; one PR each, arriving with the problem that proves it. | M each | **P0** with the problem |
| F4 | ☐ **Corner cases met** — a teal dot on a stepper node once that act has shown a corner-case callout; header counter "cases met 3/4"; +3 XP the first time each case is seen. | The gamified loop the ask describes: the learner *collects* the edge cases instead of reading them. Data already exists (`frame.corner`). | S | P1 |
| F5 | ☑ **Hold indicator on Play** — shipped 2026-09-09 (`feat/hold-indicator`). A 2px bar across the bottom of the Play button, filling over the CURRENT frame’s real wait — `delay × hold`, read from the player rather than guessed, and re-keyed per frame so it restarts instead of continuing. Gone when paused, and never drawn when the motion dial is off, because a learner who asked for no motion did not ask for a smaller one. `aria-hidden`: it says nothing a screen reader is not already told. | Autoplay looks frozen on narrative frames (`hold: 3` waits three times as long with nothing moving), and a still screen reads as a broken one. | S | — |
| F6 | ☐ **Live cost meter** — the steps chart grows as you step (bar for the current act fills to the current frame; the others show their totals as ghosts). | Makes "cost" visible during play, not only after. Chart data is already there per act. | S | P1 |
| F7 | ☐ **Tap-to-peek on touch** — tap a closed rail to peek, tap outside to close (hover-peek is mouse-only). | Tablets get the focus mode too. | S | P1 |
| F8 | ☐ **Hash-map resize animation** — when `resized` flips, the bucket grid FLIPs from n to 2n columns with the rehash as one morph. | The doubling is the one moment the "iceberg" view exists for and it currently snaps. | M | P1 |
| F9 | ☐ **Chip trail in cinematic motion** — a fading ghost at the previous position during a FLIP move. | Cinematic mode is currently just slower. | S | P2 |
| F10 | ☐ **Structural panel kinds** — `list` (nodes + arrows), `tree`, `grid`, `dp strip`, `heap`. | Waves 2–3 of the pipeline. Each arrives with its first problem. | M–L each | P1 with the problem |
| F11 | ☐ **Practice-set StepPlayer → journey chips** (part of B1) — the static walkthrough draws with `ChipRow` and the same grammar. | Two chip grammars on one site. | S | P1 |
| F12 | ☐ **Narration typewriter** (off in `calm`/`off` motion) — the note reveals word by word within the frame's hold. | Reading pace matches the hold; cheap delight. | S | P2 |

## Shipped this round (2026-09-04)

- ☑ B4 collapsible rails + a bigger type scale on the journey page (stage 717 → 1280 px at 1440 wide).
- ☑ Third journey: Triplets Summing to Zero (`docs/PROBLEMS.md` #3) with the new `terms` panel kind.
- ☑ F2 three-language code on the practice set (type, tabs, test; 9 approaches × Java + C++).
- ☑ F1 / B22 code splitting: 673.77 kB single chunk → 400 kB index (127 kB gzip) + 204 kB shared engine/data (67 kB gzip) + lazy journey 44 kB, FLIP 21 kB, visualizer 9 kB.
- ☑ Shell: independent scroll panels on the journey page, hover-peek on closed rails, `f` focus key, settings dialog (B3), `?` shortcuts dialog (B5), help dialog, sidebar regrouped DSA / DSA · patterns / SQL / Data science with a where-you-are footer line.
- ☑ Corner cases as content: `edgeCases` per journey (4 + 4), story-act cards (how to read · bring three inputs), in-play callout on `corner`-tagged frames, gated by a test that every case is explained on its preset. New presets `tiny`, `negatives` (Two Sum), `zero` (Single Number).

- ☑ Repo merge with history (`legacy/visualizer` subtree, 47 commits preserved).
- ☑ Engine port: typed, DOM-free, view models instead of HTML strings; content gate as tests.
- ☑ HTTP API (`API.md`) mounted three ways from one route table.
- ☑ Journey page in React with the full act arc for Two Sum and Single Number, including predict,
  quiz gates, hint ladder, XP, deep links, the code challenge with trace playback, scorecard,
  self-review, second set, adaptive difficulty.
- ☑ Hash map drawn as buckets + chains + load meter; bit rows; sorted view; recap table.
- ☑ Algorithm visualizer with FLIP bars and an SVG graph view.
- ☑ Docs set: PRD, FEATURES, BACKLOG, ROADMAP, ARCHITECTURE, API, AUTHORING, WORKLOG, RESEARCH.
