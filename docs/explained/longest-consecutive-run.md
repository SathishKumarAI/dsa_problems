# Longest Consecutive Sequence — Explained, One Approach at a Time

<!-- Generated by scripts/gen-explained.mjs from src/data/problems/arrays-hashing/longest-consecutive-run.ts.
     Edit the data, then run `npm run docs:explained`. Edits here are overwritten. -->

**Arrays & Hashing** · medium · [the same question on LeetCode](https://leetcode.com/problems/longest-consecutive-sequence/)

Given an unsorted integer array, return the length of the longest sequence of consecutive integers present in it (order in the array does not matter). Target O(n) — sorting is the fallback, not the answer.

## What the input promises

- 0 <= nums.length <= 10^5
- -10^9 <= nums[i] <= 10^9
- duplicates are allowed and do not lengthen a run
- the array is not sorted, and sorting it is what the O(n) answer avoids

## Examples

| Input | Output | Why |
|---|---|---|
| `nums = [50, 3, 2, 100, 4, 1]` | `4` | 1, 2, 3, 4 is the longest run. |
| `nums = []` | `0` | — |

<details>
<summary>Hints, one nudge at a time — open them in order</summary>

1. Put everything in a set. Membership checks are now O(1).
2. If you start counting from the middle of a run you will re-count it many times. Which numbers are safe to start from?
3. A number x starts a run only if x - 1 is absent from the set. Only start counting there.

</details>

---

## 1. Sort

Sort, then walk once counting runs (skip duplicates, reset on gaps). Violates the O(n) target but is the honest first answer and handles everything correctly.

```python
def longest_consecutive(nums: list[int]) -> int:
    if not nums:
        return 0
    nums = sorted(set(nums))
    best = run = 1
    for prev, cur in zip(nums, nums[1:]):
        run = run + 1 if cur == prev + 1 else 1
        best = max(best, run)
    return best
```

**O(n log n) time · O(1) space**
<details>
<summary>The same rung in Java</summary>

```java
public int longestConsecutive(int[] nums) {
    if (nums.length == 0) return 0;
    Set<Integer> set = new HashSet<>();
    for (int n : nums) set.add(n);
    Integer[] arr = set.toArray(new Integer[0]);
    Arrays.sort(arr);
    int best = 1, run = 1;
    for (int i = 0; i < arr.length - 1; i++) {
        int prev = arr[i];
        int cur = arr[i + 1];
        if (cur == prev + 1) run++;
        else run = 1;
        best = Math.max(best, run);
    }
    return best;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int longestConsecutive(const vector<int>& nums) {
    if (nums.empty()) return 0;
    unordered_set<int> s(nums.begin(), nums.end());
    vector<int> v(s.begin(), s.end());
    sort(v.begin(), v.end());
    int best = 1, run = 1;
    for (int i = 0; i + 1 < (int)v.size(); ++i) {
        int prev = v[i];
        int cur = v[i + 1];
        if (cur == prev + 1) run++;
        else run = 1;
        best = max(best, run);
    }
    return best;
}
```

</details>

---

## 2. The one to remember

> **Why now.** Sorting spends n log n to learn something a set already knows - whether a number is present. Ask that question directly, start a walk only from a value with no left neighbour, and the whole thing is linear.

Load all values into a hash set. For each value x, if x - 1 is not in the set then x is the left end of a run — walk x+1, x+2, … while they exist and track the length. Every element is visited at most twice (once in the outer loop, once inside a walk), so the total work is linear despite the nested-looking loops.

```python
def longest_consecutive(nums: list[int]) -> int:
    values = set(nums)
    best = 0
    for x in values:
        if x - 1 in values:
            continue  # not the start of a run
        length = 1
        while x + length in values:
            length += 1
        best = max(best, length)
    return best
```

**O(n) time · O(n) space**
<details>
<summary>The same rung in Java</summary>

```java
public int longestConsecutive(int[] nums) {
    Set<Integer> values = new HashSet<>();
    for (int num : nums) values.add(num);
    int best = 0;
    for (int x : values) {
        if (values.contains(x - 1)) continue;
        int length = 1;
        while (values.contains(x + length)) length++;
        best = Math.max(best, length);
    }
    return best;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int longestConsecutive(const vector<int>& nums) {
    unordered_set<int> values;
    for (int num : nums) values.insert(num);
    int best = 0;
    for (int x : values) {
        if (values.find(x - 1) != values.end()) continue;
        int length = 1;
        while (values.find(x + length) != values.end()) length++;
        best = max(best, length);
    }
    return best;
}
```

</details>

---

## The arc

Sorting is the obvious way to make neighbours adjacent, and it is also the thing to question: the answer needs to know which values EXIST, not what order they come in. Once every value is in a set, the run starting at v can be walked directly — and the trick that keeps it linear is refusing to start a walk at a value whose predecessor exists, so each run is walked exactly once from its true beginning. Without that guard the same run is re-walked from every member and the whole thing is quadratic. Carry two habits: when a solution sorts, ask whether membership would do instead; and when several starting points share work, find the rule that elects one of them.

| # | Approach | Cost | What it adds |
|---|---|---|---|
| 1 | Sort | O(n log n) time · O(1) space | the baseline — nothing before it |
| 2 | The one to remember | O(n) time · O(n) space | Sorting spends n log n to learn something a set already knows - whether a number is present. Ask that question directly, start a walk only from a value with no left neighbour, and the whole thing is linear. |

---

## Full runnable script

Every rung in one file, printed against the inputs `npm run verify:run`
drives the Java and C++ blocks with. Run it: `python explained.py`.

```python
def longest_consecutive_sort(nums: list[int]) -> int:
    if not nums:
        return 0
    nums = sorted(set(nums))
    best = run = 1
    for prev, cur in zip(nums, nums[1:]):
        run = run + 1 if cur == prev + 1 else 1
        best = max(best, run)
    return best


def longest_consecutive_the_one_to_remember(nums: list[int]) -> int:
    values = set(nums)
    best = 0
    for x in values:
        if x - 1 in values:
            continue  # not the start of a run
        length = 1
        while x + length in values:
            length += 1
        best = max(best, length)
    return best

APPROACHES = [
    longest_consecutive_sort,
    longest_consecutive_the_one_to_remember,
]

# the same inputs npm run verify:run drives the Java and C++ blocks with:
# a run that must be found from its START, not from any member — [100,4,200,1,3,2]
CASES = [
    lambda: ([100,4,200,1,3,2],),
    lambda: ([],),
    lambda: ([1,2,0,1],),
    lambda: ([-5,-4,-3,9],),
]

if __name__ == "__main__":
    for fn in APPROACHES:
        answers = []
        for make in CASES:
            try:
                answers.append(fn(*make()))
            except BaseException as exc:  # a rung that raises names itself
                answers.append(f"!{exc}")
        print(f"{fn.__name__:<41} -> {answers}")
```
