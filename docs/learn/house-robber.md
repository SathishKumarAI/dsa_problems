# Non-Adjacent Maximum Take

<!-- Generated by scripts/gen-learn.mjs from src/data/problems/dp/house-robber.ts
     (no deep document yet — see docs/BACKLOG.md).
     Edit those, then run `npm run docs:learn`. Edits here are overwritten. -->

**Dynamic Programming** · medium · [the same question on LeetCode](https://leetcode.com/problems/house-robber/)

> Everything about this problem is on this page: the question, what the input
> promises, the hints, every approach in Python, Java and C++,
> and a script you can run. It **does not hide anything** — if you are partway
> through the journey for this problem, it will show you the ending.

Given non-negative values in a row, choose a subset with no two adjacent elements so the sum is maximised. Return the sum.

## What the input promises

- 1 <= nums.length <= 100
- 0 <= nums[i] <= 400
- no two chosen indices may be adjacent

## Examples

| Input | Output | Why |
|---|---|---|
| `nums = [2, 7, 9, 3, 1]` | `12` | 2 + 9 + 1. |

<details>
<summary>Hints, one nudge at a time — open them in order</summary>

1. At each position: take it (plus best up to i-2) or skip it (best up to i-1).
2. best(i) = max(best(i-1), nums[i] + best(i-2)).
3. Two rolling variables again — the array version is training wheels.

</details>

---

## The long explanation is not written yet

This problem has no document in `docs/deep/` — 81 of 127 do. What follows
is the ladder itself: every approach in build order, each with the weakness in the
one before it, in all three languages.


---

## Rung 1 — Recursion + memo

best(i) computed top-down with a cache, so each index is solved once. The recurrence is already the final one; this rung exists to show that the exponential version and the linear version differ by a dictionary. The cost still carried is the stack — one frame per house — which the table below removes by asking the questions in an order that needs no stack.

```python
from functools import lru_cache

def max_take(nums: list[int]) -> int:
    @lru_cache(maxsize=None)
    def best(i: int) -> int:
        if i < 0:
            return 0
        return max(best(i - 1), nums[i] + best(i - 2))

    return best(len(nums) - 1)
```

**O(n) time · O(n) space**
<details>
<summary>The same rung in Java</summary>

```java
public int maxTake(int[] nums) {
    Integer[] memo = new Integer[nums.length];
    return best(nums, nums.length - 1, memo);
}
private int best(int[] nums, int i, Integer[] memo) {
    if (i < 0) return 0;
    if (memo[i] != null) return memo[i];
    int take = nums[i] + best(nums, i - 2, memo);
    int skip = best(nums, i - 1, memo);
    memo[i] = Math.max(take, skip);
    return memo[i];
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int maxTake(const vector<int>& nums) {
    vector<int> memo(nums.size(), INT_MIN);
    function<int(int)> best = [&](int i) -> int {
        if (i < 0) return 0;
        if (memo[i] != INT_MIN) return memo[i];
        int take = nums[i] + best(i - 2);
        int skip = best(i - 1);
        memo[i] = max(take, skip);
        return memo[i];
    };
    return best((int)nums.size() - 1);
}
```

</details>

---

## Rung 2 — Full table

> **Why now.** The memo already computes bottom-up, with a call stack in the way. Filling the array in order removes the recursion and makes the order of computation visible.

The same answers in an explicit array, filled front to back. No recursion and nothing to overflow, and it is the version to write when you need to recover WHICH houses were taken, because the table is a record of the decisions. The waste is that the transition reads only the previous two entries, so n slots are kept to serve a window of two.

```python
def max_take(nums: list[int]) -> int:
    if not nums:
        return 0
    dp = [0] * (len(nums) + 2)
    for i, x in enumerate(nums):
        dp[i + 2] = max(dp[i + 1], x + dp[i])
    return dp[-1]
```

**O(n) time · O(n) space**
<details>
<summary>The same rung in Java</summary>

```java
public int maxTake(int[] nums) {
    if (nums.length == 0) return 0;
    int[] dp = new int[nums.length + 2];
    for (int i = 0; i < nums.length; i++) {
        int x = nums[i];
        dp[i + 2] = Math.max(dp[i + 1], x + dp[i]);
    }
    return dp[dp.length - 1];
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int maxTake(const vector<int>& nums) {
    if (nums.size() == 0) return 0;
    vector<int> dp((int)nums.size() + 2, 0);
    for (int i = 0; i < (int)nums.size(); i++) {
        int x = nums[i];
        dp[i + 2] = max(dp[i + 1], x + dp[i]);
    }
    return dp.back();
}
```

</details>

---

## Rung 3 — The one to remember

> **Why now.** The table keeps n values, but each step reads only the two before it. Two variables are enough, and the space drops to constant.

State: best(i), the maximum sum using only the first i+1 elements. Transition: either element i is skipped (carry best(i-1)) or taken (nums[i] + best(i-2), since i-1 is then forbidden). Take the max. Roll two variables left to right; the final value is the answer.

```python
def max_take(nums: list[int]) -> int:
    skip = take = 0  # best excluding / including previous element
    for x in nums:
        skip, take = max(skip, take), skip + x
    return max(skip, take)
```

**O(n) time · O(1) space**
<details>
<summary>The same rung in Java</summary>

```java
public int maxTake(int[] nums) {
    int skip = 0, take = 0;
    for (int x : nums) {
        int newSkip = Math.max(skip, take);
        int newTake = skip + x;
        skip = newSkip;
        take = newTake;
    }
    return Math.max(skip, take);
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int maxTake(const vector<int>& nums) {
    int skip = 0, take = 0;
    for (int x : nums) {
        int newSkip = max(skip, take);
        int newTake = skip + x;
        skip = newSkip;
        take = newTake;
    }
    return max(skip, take);
}
```

</details>

---

## The arc

The state is the whole problem, and it is smaller than it looks: standing at element i, the only thing the past can still tell you is the best total using everything before it, so best(i) is either best(i−1) with i skipped or nums[i] plus best(i−2) with i taken. Nothing about WHICH elements were chosen ever matters again, and that collapse from a subset to one number per position is what turns an exponential choice tree into a linear walk. The rest is the usual descent. Memoising asks each position once but leaves a call stack in the way; the explicit table removes the stack and makes the fill order visible, and it is the version to keep when a follow-up asks which elements were taken; and since the recurrence reads only two cells back, two rolling variables finish it in constant space. Know the two-variable version cold and practise naming the state out loud — the circular and tree variants are this recurrence with a different neighbour rule.

| # | Approach | Cost | What it adds |
|---|---|---|---|
| 1 | Recursion + memo | O(n) time · O(n) space | the baseline — nothing before it |
| 2 | Full table | O(n) time · O(n) space | The memo already computes bottom-up, with a call stack in the way. Filling the array in order removes the recursion and makes the order of computation visible. |
| 3 | The one to remember | O(n) time · O(1) space | The table keeps n values, but each step reads only the two before it. Two variables are enough, and the space drops to constant. |

---

## Where this sits

**Pattern — Dynamic Programming.** Overlapping subproblems: define state, write the recurrence, fill the table.

The other 13 problems on the same pattern:

[Ways to Climb Stairs](stair-ways.md) · [Fewest Coins for Amount](coin-change-min.md) · [Longest Increasing Subsequence](longest-increasing-run.md) · [Best Contiguous Run](max-subarray.md) · [Longest Shared Subsequence](longest-common-subsequence.md) · [Set Bits for Every Number up to n](counting-bits.md) · [Split the Array Into Two Equal Halves](partition-equal-subset.md) · [Paths Across a Grid](unique-paths.md) · [Can the Sentence Be Cut Into Words?](word-break.md) · [Cheapest Way Up the Stairs](min-cost-stairs.md) · [How Many Ways to Read the Digits](decode-ways.md) · [Can You Reach the Last Index?](jump-game.md) · [The Best Product a Run Can Make](max-product-subarray.md)

**Drills and outside reading** for this pattern: [`docs/RESOURCES.md`](../RESOURCES.md).
**The interactive version**, which gates each approach until you have earned it:
run the app and open this problem.


---

## Full runnable script

Every rung in one file, printed against the inputs `npm run verify:run`
drives the Java and C++ blocks with. Run it: `python learn.py`.

```python
from functools import lru_cache

def max_take_recursion_memo(nums: list[int]) -> int:
    @lru_cache(maxsize=None)
    def best(i: int) -> int:
        if i < 0:
            return 0
        return max(best(i - 1), nums[i] + best(i - 2))

    return best(len(nums) - 1)


def max_take_full_table(nums: list[int]) -> int:
    if not nums:
        return 0
    dp = [0] * (len(nums) + 2)
    for i, x in enumerate(nums):
        dp[i + 2] = max(dp[i + 1], x + dp[i])
    return dp[-1]


def max_take_the_one_to_remember(nums: list[int]) -> int:
    skip = take = 0  # best excluding / including previous element
    for x in nums:
        skip, take = max(skip, take), skip + x
    return max(skip, take)

APPROACHES = [
    max_take_recursion_memo,
    max_take_full_table,
    max_take_the_one_to_remember,
]

# the same inputs npm run verify:run drives the Java and C++ blocks with:
# the skip being worth more than the take — a large value two positions away
CASES = [
    lambda: ([1,2,3,1],),
    lambda: ([2,7,9,3,1],),
    lambda: ([5],),
    lambda: ([0,0],),
]

if __name__ == "__main__":
    for fn in APPROACHES:
        answers = []
        for make in CASES:
            try:
                answers.append(fn(*make()))
            except BaseException as exc:  # a rung that raises names itself
                answers.append(f"!{exc}")
        print(f"{fn.__name__:<30} -> {answers}")
```
