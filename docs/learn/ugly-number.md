# The nth Number Built Only From 2, 3 and 5

<!-- Generated by scripts/gen-learn.mjs from src/data/problems/heaps/ugly-number.ts
     (no deep document yet — see docs/BACKLOG.md).
     Edit those, then run `npm run docs:learn`. Edits here are overwritten. -->

**Heaps** · medium · [the same question on LeetCode](https://leetcode.com/problems/ugly-number-ii/)

> Everything about this problem is on this page: the question, what the input
> promises, the hints, every approach in Python, Java and C++,
> and a script you can run. It **does not hide anything** — if you are partway
> through the journey for this problem, it will show you the ending.

A number is 'ugly' when its only prime factors are 2, 3 and 5. By convention 1 is the first such number. Given n, return the nth of them in increasing order.

## What the input promises

- 1 <= n <= 1690, and the 1690th value still fits in a 32-bit integer
- 1 counts as the first ugly number — it has no prime factors at all, so nothing forbidden divides it
- the sequence is strictly increasing, so duplicates have to be suppressed however they are generated
- every ugly number after the first is an earlier ugly number multiplied by 2, 3 or 5 — which is what makes generating cheaper than testing
- 6 is reachable as 2 × 3 and as 3 × 2, so any generator must decide what to do when two routes produce one value

## Examples

| Input | Output | Why |
|---|---|---|
| `n = 10` | `12` | 1, 2, 3, 4, 5, 6, 8, 9, 10, 12 — note that 7 and 11 are missing. |
| `n = 1` | `1` | — |
| `n = 7` | `8` | 6 is produced twice (2 × 3 and 3 × 2). Counting it twice shifts every later answer by one. |

<details>
<summary>Hints, one nudge at a time — open them in order</summary>

1. Testing each integer for ugliness wastes almost all of its work: past 100 the ugly numbers are rare and getting rarer.
2. Turn it around — build them. Every ugly number is 2, 3 or 5 times an earlier one, so the sequence generates itself.
3. The next value is the smallest of three candidates: 2 × (the earliest value not yet doubled), 3 × (…tripled), 5 × (…quintupled). Advance every pointer that produced it.

</details>

---

## The long explanation is not written yet

This problem has no document in `docs/deep/` — 81 of 127 do. What follows
is the ladder itself: every approach in build order, each with the weakness in the
one before it, in all three languages.


---

## Rung 1 — Test every integer

Walk the integers from 1 upward, dividing each by 2, 3 and 5 as far as they go and keeping the ones that leave 1. It executes the definition directly, and the ugly numbers thin out fast: they are sparse enough that reaching the far end of the constraint means testing billions of integers that are not ugly to collect a few thousand that are.

```python
def nth_ugly(n: int) -> int:
    def is_ugly(x: int) -> bool:
        for factor in (2, 3, 5):
            while x % factor == 0:
                x //= factor
        return x == 1

    found = 0
    x = 0
    while found < n:
        x += 1
        if is_ugly(x):
            found += 1
    return x
```

**O(m log m) over the integers scanned time · O(1) space**
<details>
<summary>The same rung in Java</summary>

```java
public int nthUgly(int n) {
    int found = 0, x = 0;
    while (found < n) {
        x++;
        int rest = x;
        int[] factors = {2, 3, 5};
        for (int factor : factors)
            while (rest % factor == 0) rest /= factor;
        if (rest == 1) found++;
    }
    return x;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int nthUgly(int n) {
    int found = 0, x = 0;
    while (found < n) {
        x++;
        int rest = x;
        int factors[3] = {2, 3, 5};
        for (int i = 0; i < 3; i++)
            while (rest % factors[i] == 0) rest /= factors[i];
        if (rest == 1) found++;
    }
    return x;
}
```

</details>

---

## Rung 2 — A heap of candidates

> **Why now.** Ugly numbers thin out fast — the 1690th is over a billion, so testing integers one by one tests a billion numbers to find 1690 of them, and almost every test fails. Every ugly number is 2, 3 or 5 times a smaller one, so they can be BUILT in order instead of filtered out of the integers.

Generate instead of test: pop the smallest value from a min-heap, push it times 2, 3 and 5, and use a seen-set so a value produced by two routes is only counted once.

```python
import heapq

def nth_ugly(n: int) -> int:
    heap = [1]
    seen = {1}
    value = 1
    for _ in range(n):
        value = heapq.heappop(heap)
        for factor in (2, 3, 5):
            nxt = value * factor
            if nxt not in seen:      # 6 arrives from 2 and from 3
                seen.add(nxt)
                heapq.heappush(heap, nxt)
    return value
```

**O(n log n) time · O(n) space**
<details>
<summary>The same rung in Java</summary>

```java
public int nthUgly(int n) {
    PriorityQueue<Long> heap = new PriorityQueue<>();
    Set<Long> seen = new HashSet<>();
    heap.add(1L);
    seen.add(1L);
    long value = 1;
    int[] factors = {2, 3, 5};
    for (int i = 0; i < n; i++) {
        value = heap.poll();
        for (int factor : factors) {
            long next = value * factor;
            if (seen.add(next)) heap.add(next);
        }
    }
    return (int) value;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int nthUgly(int n) {
    priority_queue<long long, vector<long long>, greater<long long>> heap;
    unordered_set<long long> seen;
    heap.push(1);
    seen.insert(1);
    long long value = 1;
    long long factors[3] = {2, 3, 5};
    for (int i = 0; i < n; i++) {
        value = heap.top();
        heap.pop();
        for (int f = 0; f < 3; f++) {
            long long next = value * factors[f];
            if (seen.insert(next).second) heap.push(next);
        }
    }
    return (int) value;
}
```

</details>

---

## Rung 3 — The one to remember

> **Why now.** A heap with a seen-set produces the sequence in order, but it pays a logarithm per value and stores up to three candidates per number extracted, and the duplicate 6 has to be recognised by memory. The three multipliers only ever consume the sequence in order, so each needs nothing more than an INDEX into what has already been produced — the heap collapses to three integers, and the duplicate disappears because both pointers advance on the same value instead of being remembered.

Keep the sequence in an array and three indices, one per multiplier. The next value is the smallest of 2 × seq[i2], 3 × seq[i3] and 5 × seq[i5]; append it, then advance EVERY index whose product equals it — that is what stops 6 from being emitted twice. Each value costs three multiplications and three comparisons, so the whole sequence is linear in n with no ordering structure at all.

```python
def nth_ugly(n: int) -> int:
    seq = [1]
    i2 = i3 = i5 = 0
    while len(seq) < n:
        by2, by3, by5 = seq[i2] * 2, seq[i3] * 3, seq[i5] * 5
        nxt = min(by2, by3, by5)
        seq.append(nxt)
        # every pointer that produced it moves — 6 appears once, not twice
        if nxt == by2:
            i2 += 1
        if nxt == by3:
            i3 += 1
        if nxt == by5:
            i5 += 1
    return seq[n - 1]
```

**O(n) time · O(n) space**
<details>
<summary>The same rung in Java</summary>

```java
public int nthUgly(int n) {
    int[] seq = new int[n];
    seq[0] = 1;
    int i2 = 0, i3 = 0, i5 = 0;
    for (int k = 1; k < n; k++) {
        int by2 = seq[i2] * 2, by3 = seq[i3] * 3, by5 = seq[i5] * 5;
        int next = Math.min(by2, Math.min(by3, by5));
        seq[k] = next;
        if (next == by2) i2++;
        if (next == by3) i3++;
        if (next == by5) i5++;
    }
    return seq[n - 1];
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int nthUgly(int n) {
    vector<int> seq(n);
    seq[0] = 1;
    int i2 = 0, i3 = 0, i5 = 0;
    for (int k = 1; k < n; k++) {
        int by2 = seq[i2] * 2, by3 = seq[i3] * 3, by5 = seq[i5] * 5;
        int next = min(by2, min(by3, by5));
        seq[k] = next;
        if (next == by2) i2++;
        if (next == by3) i3++;
        if (next == by5) i5++;
    }
    return seq[n - 1];
}
```

</details>

---

## The arc

A test becomes a generator, and that is the whole idea. Checking integers one by one wastes almost all its work because the answers thin out — past a billion they are vanishingly rare — while every answer is 2, 3 or 5 times an earlier answer, so the sequence can build itself. Once you are generating, the heap is the obvious way to keep order and the three pointers are the observation that each multiplier only ever consumes the sequence in order, so a heap is more machinery than the job needs. Carry two things: when the valid values are sparse, generate instead of filter; and when a value can be produced by more than one route, decide deliberately whether to deduplicate by memory (a seen-set) or by construction (advance every pointer that tied) — the second is cheaper and is the reason 6 appears exactly once here.

| # | Approach | Cost | What it adds |
|---|---|---|---|
| 1 | Test every integer | O(m log m) over the integers scanned time · O(1) space | the baseline — nothing before it |
| 2 | A heap of candidates | O(n log n) time · O(n) space | Ugly numbers thin out fast — the 1690th is over a billion, so testing integers one by one tests a billion numbers to find 1690 of them, and almost every test fails. Every ugly number is 2, 3 or 5 times a smaller one, so they can be BUILT in order instead of filtered out of the integers. |
| 3 | The one to remember | O(n) time · O(n) space | A heap with a seen-set produces the sequence in order, but it pays a logarithm per value and stores up to three candidates per number extracted, and the duplicate 6 has to be recognised by memory. The three multipliers only ever consume the sequence in order, so each needs nothing more than an INDEX into what has already been produced — the heap collapses to three integers, and the duplicate disappears because both pointers advance on the same value instead of being remembered. |

---

## Where this sits

**Pattern — Heaps.** Repeated min/max of a changing set: a heap gives it in O(log n) per operation.

The other 8 problems on the same pattern:

[Kth Largest in a Stream](kth-largest-stream.md) · [K Closest Points to Origin](k-closest-points.md) · [Task Scheduling With Cooldown](task-cooldown.md) · [Smash the Two Heaviest Stones](last-stone-weight.md) · [The kth Largest Value](kth-largest-element.md) · [Sort Characters by How Often They Appear](sort-by-frequency.md) · [kth Smallest in a Sorted Matrix](kth-smallest-matrix.md) · [The k Most Common Words, Ties Alphabetical](top-k-frequent-words.md)

**Drills and outside reading** for this pattern: [`docs/RESOURCES.md`](../RESOURCES.md).
**The interactive version**, which gates each approach until you have earned it:
run the app and open this problem.


---

## Full runnable script

Every rung in one file, printed against the inputs `npm run verify:run`
drives the Java and C++ blocks with. Run it: `python learn.py`.

```python
def nth_ugly_test_every_integer(n: int) -> int:
    def is_ugly(x: int) -> bool:
        for factor in (2, 3, 5):
            while x % factor == 0:
                x //= factor
        return x == 1

    found = 0
    x = 0
    while found < n:
        x += 1
        if is_ugly(x):
            found += 1
    return x


import heapq

def nth_ugly_a_heap_of_candidates(n: int) -> int:
    heap = [1]
    seen = {1}
    value = 1
    for _ in range(n):
        value = heapq.heappop(heap)
        for factor in (2, 3, 5):
            nxt = value * factor
            if nxt not in seen:      # 6 arrives from 2 and from 3
                seen.add(nxt)
                heapq.heappush(heap, nxt)
    return value


def nth_ugly_the_one_to_remember(n: int) -> int:
    seq = [1]
    i2 = i3 = i5 = 0
    while len(seq) < n:
        by2, by3, by5 = seq[i2] * 2, seq[i3] * 3, seq[i5] * 5
        nxt = min(by2, by3, by5)
        seq.append(nxt)
        # every pointer that produced it moves — 6 appears once, not twice
        if nxt == by2:
            i2 += 1
        if nxt == by3:
            i3 += 1
        if nxt == by5:
            i5 += 1
    return seq[n - 1]

APPROACHES = [
    nth_ugly_test_every_integer,
    nth_ugly_a_heap_of_candidates,
    nth_ugly_the_one_to_remember,
]

# the same inputs npm run verify:run drives the Java and C++ blocks with:
# the value two routes produce — n = 7 lands on 8 only when 6 is emitted ONCE, so advancing a single pointer on a tie shifts every later answer
CASES = [
    lambda: (10,),
    lambda: (1,),
    lambda: (7,),
    lambda: (2,),
    lambda: (11,),
    lambda: (150,),
]

if __name__ == "__main__":
    for fn in APPROACHES:
        answers = []
        for make in CASES:
            try:
                answers.append(fn(*make()))
            except BaseException as exc:  # a rung that raises names itself
                answers.append(f"!{exc}")
        print(f"{fn.__name__:<31} -> {answers}")
```
