# Can You Reach the Last Index?

<!-- Generated by scripts/gen-learn.mjs from src/data/problems/dp/jump-game.ts
     (no deep document yet — see docs/BACKLOG.md).
     Edit those, then run `npm run docs:learn`. Edits here are overwritten. -->

**Dynamic Programming** · medium · [the same question on LeetCode](https://leetcode.com/problems/jump-game/)

> Everything about this problem is on this page: the question, what the input
> promises, the hints, every approach in Python, Java and C++,
> and a script you can run. It **does not hide anything** — if you are partway
> through the journey for this problem, it will show you the ending.

You start at index 0 of an array. The value at an index is the maximum number of steps you may jump forward from it. Return true when some sequence of jumps reaches the last index.

## What the input promises

- 1 <= nums.length <= 10^4
- 0 <= nums[i] <= 10^5
- a value is a MAXIMUM, not a fixed step — from a 3 you may jump 1, 2 or 3
- a zero is a wall you can only pass by jumping OVER it from an earlier index
- an array of length 1 is already at the end, so the answer is true even when that single value is 0

## Examples

| Input | Output | Why |
|---|---|---|
| `nums = [2, 3, 1, 1, 4]` | `true` | 1 step to index 1, then 3 steps to the end. Taking the full 2 from the start also works. |
| `nums = [3, 2, 1, 0, 4]` | `false` | Every route lands on the 0 at index 3 and stops there. The 4 beyond it is unreachable. |
| `nums = [0]` | `true` | Already standing on the last index. |

<details>
<summary>Hints, one nudge at a time — open them in order</summary>

1. You never need to know WHICH jumps were taken — only whether the end is reachable at all.
2. Walking left to right, keep the furthest index reached so far. An index is standable when it is not past that reach.
3. The moment the furthest reach falls behind the index you are standing on, everything after it is unreachable and you can stop.

</details>

---

## The long explanation is not written yet

This problem has no document in `docs/deep/` — 81 of 127 do. What follows
is the ladder itself: every approach in build order, each with the weakness in the
one before it, in all three languages.


---

## Rung 1 — Try every jump length

Backtracking from index 0: from each index try every jump from 1 up to its value, and report success as soon as any route lands on the last index.

```python
def can_jump(nums: list[int]) -> bool:
    last = len(nums) - 1

    def from_index(i: int) -> bool:
        if i >= last:
            return True
        for step in range(1, nums[i] + 1):
            if from_index(i + step):
                return True
        return False

    return from_index(0)
```

**O(2^n) time · O(n) space**
<details>
<summary>The same rung in Java</summary>

```java
public boolean canJump(int[] nums) {
    return fromIndex(nums, 0);
}

private boolean fromIndex(int[] nums, int i) {
    if (i >= nums.length - 1) return true;
    for (int step = 1; step <= nums[i]; step++)
        if (fromIndex(nums, i + step)) return true;
    return false;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
bool fromIndex(const vector<int>& nums, int i) {
    if (i >= (int)nums.size() - 1) return true;
    for (int step = 1; step <= nums[i]; step++)
        if (fromIndex(nums, i + step)) return true;
    return false;
}

bool canJump(vector<int> nums) {
    return fromIndex(nums, 0);
}
```

</details>

---

## Rung 2 — A good/bad table

> **Why now.** Backtracking re-explores the same index through every route that reaches it, so an array like [5,4,3,2,1,…] revisits the tail exponentially often. Whether an index can reach the end does not depend on how you got to it — one boolean per index, computed once, replaces the whole tree.

Fill a table right to left: an index is good when some jump from it lands on a good index. The answer is whether index 0 is good.

```python
def can_jump(nums: list[int]) -> bool:
    n = len(nums)
    good = [False] * n
    good[n - 1] = True
    for i in range(n - 2, -1, -1):
        furthest = min(i + nums[i], n - 1)
        for j in range(i + 1, furthest + 1):
            if good[j]:
                good[i] = True
                break
    return good[0]
```

**O(n²) time · O(n) space**
<details>
<summary>The same rung in Java</summary>

```java
public boolean canJump(int[] nums) {
    int n = nums.length;
    boolean[] good = new boolean[n];
    good[n - 1] = true;
    for (int i = n - 2; i >= 0; i--) {
        int furthest = Math.min(i + nums[i], n - 1);
        for (int j = i + 1; j <= furthest; j++) {
            if (good[j]) {
                good[i] = true;
                break;
            }
        }
    }
    return good[0];
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
bool canJump(vector<int> nums) {
    int n = (int)nums.size();
    vector<bool> good(n, false);
    good[n - 1] = true;
    for (int i = n - 2; i >= 0; i--) {
        int furthest = min(i + nums[i], n - 1);
        for (int j = i + 1; j <= furthest; j++) {
            if (good[j]) {
                good[i] = true;
                break;
            }
        }
    }
    return good[0];
}
```

</details>

---

## Rung 3 — The leftmost good index

> **Why now.** The table scans forward from every index to check whether ANY good index is in range — but the good indices form a suffix-anchored set whose leftmost member is all that matters: if a jump reaches the leftmost good index it reaches the end, and if it does not reach that one it reaches none. One number replaces the whole row and the inner loop.

Walk right to left keeping one number: the leftmost index known to reach the end. An index joins it when its jump covers that target, and the target moves to it.

```python
def can_jump(nums: list[int]) -> bool:
    target = len(nums) - 1
    for i in range(len(nums) - 2, -1, -1):
        if i + nums[i] >= target:
            target = i
    return target == 0
```

**O(n) time · O(1) space**
<details>
<summary>The same rung in Java</summary>

```java
public boolean canJump(int[] nums) {
    int target = nums.length - 1;
    for (int i = nums.length - 2; i >= 0; i--)
        if (i + nums[i] >= target) target = i;
    return target == 0;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
bool canJump(vector<int> nums) {
    int target = (int)nums.size() - 1;
    for (int i = (int)nums.size() - 2; i >= 0; i--)
        if (i + nums[i] >= target) target = i;
    return target == 0;
}
```

</details>

---

## Rung 4 — The one to remember

> **Why now.** Scanning backwards for the nearest good index is linear, but it reads the array right to left while the problem runs left to right, and it keeps the answer in terms of a moving target index. Carrying the furthest reachable index forward answers the same question in one left-to-right pass with a single integer of state, and it can stop the moment the reach falls behind — which is the same failure the backwards scan only discovers at the end.

One pass, one number: the furthest index reachable so far, starting at 0. At each index, if it is beyond the current reach, no sequence of jumps arrives there and the answer is false. Otherwise extend the reach to the larger of itself and index + nums[index]. If the loop survives to the end — or the reach ever covers the last index — the end is reachable. The greedy is safe because reach is a maximum over everything seen, so it never overstates what is possible and never forgets a better jump.

```python
def can_jump(nums: list[int]) -> bool:
    reach = 0
    for i, step in enumerate(nums):
        if i > reach:
            return False        # a gap nothing jumps over
        reach = max(reach, i + step)
    return True
```

**O(n) time · O(1) space**
<details>
<summary>The same rung in Java</summary>

```java
public boolean canJump(int[] nums) {
    int reach = 0;
    for (int i = 0; i < nums.length; i++) {
        if (i > reach) return false;
        reach = Math.max(reach, i + nums[i]);
    }
    return true;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
bool canJump(vector<int> nums) {
    int reach = 0;
    for (int i = 0; i < (int)nums.size(); i++) {
        if (i > reach) return false;
        reach = max(reach, i + nums[i]);
    }
    return true;
}
```

</details>

---

## The arc

Notice what the question does NOT ask: it never wants the jumps, only whether the end is reachable. Every rung that tries to construct a route — backtracking, then a table of good indices — is answering a harder question than the one asked, and pays for it. Once you keep only the furthest index reachable so far, the whole problem collapses into one number and one comparison. That is the greedy test in general: find a quantity that is a maximum over everything seen, and check that extending it never invalidates an earlier choice. Know this one cold, and know its sibling — Jump Game II, which asks for the FEWEST jumps and needs the same reach plus a second boundary marking where the current jump ends.

| # | Approach | Cost | What it adds |
|---|---|---|---|
| 1 | Try every jump length | O(2^n) time · O(n) space | the baseline — nothing before it |
| 2 | A good/bad table | O(n²) time · O(n) space | Backtracking re-explores the same index through every route that reaches it, so an array like [5,4,3,2,1,…] revisits the tail exponentially often. Whether an index can reach the end does not depend on how you got to it — one boolean per index, computed once, replaces the whole tree. |
| 3 | The leftmost good index | O(n) time · O(1) space | The table scans forward from every index to check whether ANY good index is in range — but the good indices form a suffix-anchored set whose leftmost member is all that matters: if a jump reaches the leftmost good index it reaches the end, and if it does not reach that one it reaches none. One number replaces the whole row and the inner loop. |
| 4 | The one to remember | O(n) time · O(1) space | Scanning backwards for the nearest good index is linear, but it reads the array right to left while the problem runs left to right, and it keeps the answer in terms of a moving target index. Carrying the furthest reachable index forward answers the same question in one left-to-right pass with a single integer of state, and it can stop the moment the reach falls behind — which is the same failure the backwards scan only discovers at the end. |

---

## Where this sits

**Pattern — Dynamic Programming.** Overlapping subproblems: define state, write the recurrence, fill the table.

The other 13 problems on the same pattern:

[Ways to Climb Stairs](stair-ways.md) · [Non-Adjacent Maximum Take](house-robber.md) · [Fewest Coins for Amount](coin-change-min.md) · [Longest Increasing Subsequence](longest-increasing-run.md) · [Best Contiguous Run](max-subarray.md) · [Longest Shared Subsequence](longest-common-subsequence.md) · [Set Bits for Every Number up to n](counting-bits.md) · [Split the Array Into Two Equal Halves](partition-equal-subset.md) · [Paths Across a Grid](unique-paths.md) · [Can the Sentence Be Cut Into Words?](word-break.md) · [Cheapest Way Up the Stairs](min-cost-stairs.md) · [How Many Ways to Read the Digits](decode-ways.md) · [The Best Product a Run Can Make](max-product-subarray.md)

**Drills and outside reading** for this pattern: [`docs/RESOURCES.md`](../RESOURCES.md).
**The interactive version**, which gates each approach until you have earned it:
run the app and open this problem.


---

## Full runnable script

Every rung in one file, printed against the inputs `npm run verify:run`
drives the Java and C++ blocks with. Run it: `python learn.py`.

```python
def can_jump_try_every_jump_length(nums: list[int]) -> bool:
    last = len(nums) - 1

    def from_index(i: int) -> bool:
        if i >= last:
            return True
        for step in range(1, nums[i] + 1):
            if from_index(i + step):
                return True
        return False

    return from_index(0)


def can_jump_a_good_bad_table(nums: list[int]) -> bool:
    n = len(nums)
    good = [False] * n
    good[n - 1] = True
    for i in range(n - 2, -1, -1):
        furthest = min(i + nums[i], n - 1)
        for j in range(i + 1, furthest + 1):
            if good[j]:
                good[i] = True
                break
    return good[0]


def can_jump_the_leftmost_good_index(nums: list[int]) -> bool:
    target = len(nums) - 1
    for i in range(len(nums) - 2, -1, -1):
        if i + nums[i] >= target:
            target = i
    return target == 0


def can_jump_the_one_to_remember(nums: list[int]) -> bool:
    reach = 0
    for i, step in enumerate(nums):
        if i > reach:
            return False        # a gap nothing jumps over
        reach = max(reach, i + step)
    return True

APPROACHES = [
    can_jump_try_every_jump_length,
    can_jump_a_good_bad_table,
    can_jump_the_leftmost_good_index,
    can_jump_the_one_to_remember,
]

# the same inputs npm run verify:run drives the Java and C++ blocks with:
# the zero that cannot be passed — [3,2,1,0,4] stalls exactly on the reach, and [0] is already at the end so a length-1 array must answer true
CASES = [
    lambda: ([2,3,1,1,4],),
    lambda: ([3,2,1,0,4],),
    lambda: ([0],),
    lambda: ([2,0,0],),
    lambda: ([1,0,1,0],),
    lambda: ([5,0,0,0,0,0],),
]

if __name__ == "__main__":
    for fn in APPROACHES:
        answers = []
        for make in CASES:
            try:
                answers.append(fn(*make()))
            except BaseException as exc:  # a rung that raises names itself
                answers.append(f"!{exc}")
        print(f"{fn.__name__:<34} -> {answers}")
```
