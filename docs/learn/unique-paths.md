# Paths Across a Grid

<!-- Generated by scripts/gen-learn.mjs from src/data/problems/dp/unique-paths.ts
     (no deep document yet — see docs/BACKLOG.md).
     Edit those, then run `npm run docs:learn`. Edits here are overwritten. -->

**Dynamic Programming** · medium · [the same question on LeetCode](https://leetcode.com/problems/unique-paths/)

> Everything about this problem is on this page: the question, what the input
> promises, the hints, every approach in Python, Java and C++,
> and a script you can run. It **does not hide anything** — if you are partway
> through the journey for this problem, it will show you the ending.

A robot starts at the top-left of an m x n grid and may only move right or down. Return how many distinct paths reach the bottom-right corner.

## What the input promises

- 1 <= m, n <= 100
- moves are RIGHT or DOWN only — never up or left, which is what makes the count finite
- the answer fits in a 32-bit signed integer for all allowed inputs
- a grid one cell wide or one cell tall has exactly one path

## Examples

| Input | Output | Why |
|---|---|---|
| `m = 3, n = 7` | `28` | — |
| `m = 3, n = 2` | `3` | — |

<details>
<summary>Hints, one nudge at a time — open them in order</summary>

1. How many ways reach a given cell? Only two cells can lead into it.
2. So paths(r, c) = paths(r-1, c) + paths(r, c-1) — the cell above plus the cell to the left.
3. The top row and left column have exactly one path each, which seeds the whole table.

</details>

---

## The long explanation is not written yet

This problem has no document in `docs/deep/` — 81 of 127 do. What follows
is the ladder itself: every approach in build order, each with the weakness in the
one before it, in all three languages.


---

## Rung 1 — Branch at every cell

Recurse from the start, trying a move right and a move down at each cell and summing what each returns. Exponential, and every cell is recomputed once per path that reaches it — the count for a cell depends only on the cell itself, not on the route taken to arrive.

```python
def walk(m: int, n: int, r: int, c: int) -> int:
    if r == m - 1 or c == n - 1:
        return 1
    return walk(m, n, r + 1, c) + walk(m, n, r, c + 1)


def unique_paths(m: int, n: int) -> int:
    return walk(m, n, 0, 0)
```

**O(2^(m+n)) time · O(m + n) space**
<details>
<summary>The same rung in Java</summary>

```java
public int walk(int m, int n, int r, int c) {
    if (r == m - 1 || c == n - 1) return 1;
    return walk(m, n, r + 1, c) + walk(m, n, r, c + 1);
}

public int uniquePaths(int m, int n) {
    return walk(m, n, 0, 0);
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int walk(int m, int n, int r, int c) {
    if (r == m - 1 || c == n - 1) return 1;
    return walk(m, n, r + 1, c) + walk(m, n, r, c + 1);
}

int uniquePaths(int m, int n) {
    return walk(m, n, 0, 0);
}
```

</details>

---

## Rung 2 — The one to remember

> **Why now.** The recursion asks the same cell's question once for every path that passes through it, which is exponential. Filling a table asks each cell once — and because a cell needs only the row above and the value to its left, a single row can be updated in place, so the memory drops from a full grid to one row.

Keep one row holding the number of paths to each column of the current row, initialised to all ones for the top row. Sweep down: for each new row, walk left to right adding the value to the left into the current value. That addition IS the recurrence — the value already in the slot is the count from the row above, and the value to the left is the count from the left neighbour, because it has already been updated for this row. The last entry after the final sweep is the answer.

```python
def unique_paths(m: int, n: int) -> int:
    row = [1] * n
    for _ in range(1, m):
        for c in range(1, n):
            row[c] += row[c - 1]
    return row[n - 1]
```

**O(m · n) time · O(n) space**
<details>
<summary>The same rung in Java</summary>

```java
public int uniquePaths(int m, int n) {
    int[] row = new int[n];
    Arrays.fill(row, 1);
    for (int r = 1; r < m; r++)
        for (int c = 1; c < n; c++) row[c] += row[c - 1];
    return row[n - 1];
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int uniquePaths(int m, int n) {
    vector<int> row(n, 1);
    for (int r = 1; r < m; r++)
        for (int c = 1; c < n; c++) row[c] += row[c - 1];
    return row[n - 1];
}
```

</details>

---

## The arc

The recursion asks each cell's question once per path that reaches it, and since the number of paths is the very thing being counted, that is as exponential as it sounds. Writing each cell down once makes the work m × n, and the recurrence could hardly be smaller: paths to a cell are paths to the cell above plus paths to the cell on the left, with the top row and left column at one because there is only one way to travel in a straight line. Then the compression is unusually neat — a cell needs the row above and the value to its left, and a single row swept left to right holds both at the moment they are needed, since the slot still carries the row above while the slot behind it has already been updated for this row. Adding them in place IS the recurrence. Know that in-place row trick, because it shrinks the LCS and knapsack tables the same way. This problem also has a closed form, the paths being a choice of which moves go down — and unique-paths-II kills the formula and leaves the table.

| # | Approach | Cost | What it adds |
|---|---|---|---|
| 1 | Branch at every cell | O(2^(m+n)) time · O(m + n) space | the baseline — nothing before it |
| 2 | The one to remember | O(m · n) time · O(n) space | The recursion asks the same cell's question once for every path that passes through it, which is exponential. Filling a table asks each cell once — and because a cell needs only the row above and the value to its left, a single row can be updated in place, so the memory drops from a full grid to one row. |

---

## Where this sits

**Pattern — Dynamic Programming.** Overlapping subproblems: define state, write the recurrence, fill the table.

The other 13 problems on the same pattern:

[Ways to Climb Stairs](stair-ways.md) · [Non-Adjacent Maximum Take](house-robber.md) · [Fewest Coins for Amount](coin-change-min.md) · [Longest Increasing Subsequence](longest-increasing-run.md) · [Best Contiguous Run](max-subarray.md) · [Longest Shared Subsequence](longest-common-subsequence.md) · [Set Bits for Every Number up to n](counting-bits.md) · [Split the Array Into Two Equal Halves](partition-equal-subset.md) · [Can the Sentence Be Cut Into Words?](word-break.md) · [Cheapest Way Up the Stairs](min-cost-stairs.md) · [How Many Ways to Read the Digits](decode-ways.md) · [Can You Reach the Last Index?](jump-game.md) · [The Best Product a Run Can Make](max-product-subarray.md)

**Drills and outside reading** for this pattern: [`docs/RESOURCES.md`](../RESOURCES.md).
**The interactive version**, which gates each approach until you have earned it:
run the app and open this problem.


---

## Full runnable script

Every rung in one file, printed against the inputs `npm run verify:run`
drives the Java and C++ blocks with. Run it: `python learn.py`.

```python
def walk(m: int, n: int, r: int, c: int) -> int:
    if r == m - 1 or c == n - 1:
        return 1
    return walk(m, n, r + 1, c) + walk(m, n, r, c + 1)


def unique_paths_branch_at_every_cell(m: int, n: int) -> int:
    return walk(m, n, 0, 0)


def unique_paths_the_one_to_remember(m: int, n: int) -> int:
    row = [1] * n
    for _ in range(1, m):
        for c in range(1, n):
            row[c] += row[c - 1]
    return row[n - 1]

APPROACHES = [
    unique_paths_branch_at_every_cell,
    unique_paths_the_one_to_remember,
]

# the same inputs npm run verify:run drives the Java and C++ blocks with:
# a grid one cell wide, whose answer is 1 rather than 0 — m=1; and a rectangular grid where the two dimensions differ, so a transposed recurrence would still look right on a square
CASES = [
    lambda: (3, 7),
    lambda: (3, 2),
    lambda: (1, 1),
    lambda: (1, 5),
    lambda: (2, 3),
]

if __name__ == "__main__":
    for fn in APPROACHES:
        answers = []
        for make in CASES:
            try:
                answers.append(fn(*make()))
            except BaseException as exc:  # a rung that raises names itself
                answers.append(f"!{exc}")
        print(f"{fn.__name__:<35} -> {answers}")
```
