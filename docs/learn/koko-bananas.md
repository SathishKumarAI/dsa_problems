# Slowest Sufficient Eating Speed

<!-- Generated by scripts/gen-learn.mjs from src/data/problems/binary-search/koko-bananas.ts
     (no deep document yet — see docs/BACKLOG.md).
     Edit those, then run `npm run docs:learn`. Edits here are overwritten. -->

**Binary Search** · medium · [the same question on LeetCode](https://leetcode.com/problems/koko-eating-bananas/)

> Everything about this problem is on this page: the question, what the input
> promises, the hints, every approach in Python, Java and C++,
> and a script you can run. It **does not hide anything** — if you are partway
> through the journey for this problem, it will show you the ending.

Given piles of bananas and h hours, choose the smallest integer speed k (bananas/hour) so all piles can be finished within h hours. Each hour you eat from one pile only; a pile of p bananas takes ceil(p / k) hours.

## What the input promises

- 1 <= piles.length <= 10^4
- piles.length <= h <= 10^9
- 1 <= piles[i] <= 10^9
- one pile per hour at most: an hour spent on a pile smaller than the speed is still a whole hour

## Examples

| Input | Output | Why |
|---|---|---|
| `piles = [3, 6, 7, 11], h = 8` | `4` | — |

<details>
<summary>Hints, one nudge at a time — open them in order</summary>

1. The check "can speed k finish in h hours?" is monotonic: if k works, every faster speed works.
2. Monotonic yes/no over a numeric range = binary search over that range.
3. Range is [1, max(piles)]. Find the leftmost "yes".

</details>

---

## The long explanation is not written yet

This problem has no document in `docs/deep/` — 81 of 127 do. What follows
is the ladder itself: every approach in build order, each with the weakness in the
one before it, in all three languages.


---

## Rung 1 — Try every speed

Test k = 1, 2, 3, … until one fits within h hours. The first success is the answer — correct because feasibility is monotonic, slow because the range can be huge.

```python
import math

def min_eating_speed(piles: list[int], h: int) -> int:
    k = 1
    while sum(math.ceil(p / k) for p in piles) > h:
        k += 1
    return k
```

**O(n · max(piles)) time · O(1) space**
<details>
<summary>The same rung in Java</summary>

```java
public int minEatingSpeed(int[] piles, int h) {
    int k = 1;
    while (true) {
        int total = 0;
        for (int p : piles) {
            total += (p + k - 1) / k;
        }
        if (total <= h) break;
        k++;
    }
    return k;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int minEatingSpeed(const vector<int>& piles, int h) {
    int k = 1;
    while (true) {
        long long total = 0;
        for (int p : piles) {
            total += (p + k - 1) / k;
        }
        if (total <= h) break;
        ++k;
    }
    return k;
}
```

</details>

---

## Rung 2 — The one to remember

> **Why now.** Trying speeds one at a time walks a range as wide as the largest pile. Feasibility only ever flips from no to yes once, so the answer can be binary-searched over the speed rather than over positions.

Instead of searching positions, search candidate speeds. hours(k) = Σ ceil(pile/k) is non-increasing in k, so feasibility flips from no to yes exactly once. Binary search the boundary: if hours(mid) ≤ h, mid is feasible — try slower (hi = mid); otherwise lo = mid + 1. This 'search the answer space' framing generalizes to shipping capacities, split arrays, and similar minimization problems.

```python
import math

def min_eating_speed(piles: list[int], h: int) -> int:
    def hours(k: int) -> int:
        return sum(math.ceil(p / k) for p in piles)

    lo, hi = 1, max(piles)
    while lo < hi:
        mid = (lo + hi) // 2
        if hours(mid) <= h:
            hi = mid
        else:
            lo = mid + 1
    return lo
```

**O(n log max(piles)) time · O(1) space**
<details>
<summary>The same rung in Java</summary>

```java
public int minEatingSpeed(int[] piles, int h) {
    int lo = 1;
    int hi = 0;
    for (int p : piles) if (p > hi) hi = p;
    while (lo < hi) {
        int mid = (lo + hi) / 2;
        long total = 0;
        for (int p : piles) total += (p + mid - 1) / mid;
        if (total <= h) hi = mid;
        else lo = mid + 1;
    }
    return lo;
}
```

</details>
<details>
<summary>The same rung in C++</summary>

```cpp
int minEatingSpeed(const vector<int>& piles, int h) {
    int lo = 1;
    int hi = 0;
    for (int p : piles) if (p > hi) hi = p;
    while (lo < hi) {
        int mid = (lo + hi) / 2;
        long long total = 0;
        for (int p : piles) total += (p + mid - 1) / mid;
        if (total <= h) hi = mid;
        else lo = mid + 1;
    }
    return lo;
}
```

</details>

---

## The arc

The problem that names the pattern: binary search on the ANSWER. Nothing is sorted, but the question 'is speed s fast enough' is monotone — false, false, then true forever — so the candidate speeds can be halved even though the piles cannot. The feasibility check is a plain simulation, and keeping it in its own function is what keeps the search honest. Two habits to steal: pick the bounds so every candidate is meaningful, here from one to the largest pile; and remember that the ceiling division inside the check should be done with integer arithmetic rather than floats, because a float rounding error turns a correct algorithm into an intermittent one at large inputs.

| # | Approach | Cost | What it adds |
|---|---|---|---|
| 1 | Try every speed | O(n · max(piles)) time · O(1) space | the baseline — nothing before it |
| 2 | The one to remember | O(n log max(piles)) time · O(1) space | Trying speeds one at a time walks a range as wide as the largest pile. Feasibility only ever flips from no to yes once, so the answer can be binary-searched over the speed rather than over positions. |

---

## Where this sits

**Pattern — Binary Search.** Monotonic answer space: halve it every step — search values, not just indices.

The other 10 problems on the same pattern:

[Find a Target in Sorted Array](classic-binary-search.md) · [Minimum in Rotated Sorted Array](rotated-minimum.md) · [Search a Fully Sorted Matrix](search-2d-matrix.md) · [Search a Rotated Sorted Array](rotated-search.md) · [First and Last Position of a Value](first-last-position.md) · [Where Would This Value Go?](search-insert-position.md) · [Any Local Peak, in Log Time](find-peak-element.md) · [The Lone Value Among Pairs](single-in-sorted.md) · [The k Values Nearest x](k-closest-values.md) · [The Smallest Ship That Still Makes the Deadline](ship-in-d-days.md)

**Drills and outside reading** for this pattern: [`docs/RESOURCES.md`](../RESOURCES.md).
**The interactive version**, which gates each approach until you have earned it:
run the app and open this problem.


---

## Full runnable script

Every rung in one file, printed against the inputs `npm run verify:run`
drives the Java and C++ blocks with. Run it: `python learn.py`.

```python
import math

def min_eating_speed_try_every_speed(piles: list[int], h: int) -> int:
    k = 1
    while sum(math.ceil(p / k) for p in piles) > h:
        k += 1
    return k


import math

def min_eating_speed_the_one_to_remember(piles: list[int], h: int) -> int:
    def hours(k: int) -> int:
        return sum(math.ceil(p / k) for p in piles)

    lo, hi = 1, max(piles)
    while lo < hi:
        mid = (lo + hi) // 2
        if hours(mid) <= h:
            hi = mid
        else:
            lo = mid + 1
    return lo

APPROACHES = [
    min_eating_speed_try_every_speed,
    min_eating_speed_the_one_to_remember,
]

# the same inputs npm run verify:run drives the Java and C++ blocks with:
# the answer sitting at a boundary of the feasible range — a pile that must be eaten in one hour
CASES = [
    lambda: ([3,6,7,11], 8),
    lambda: ([30,11,23,4,20], 5),
    lambda: ([30,11,23,4,20], 6),
    lambda: ([1], 1),
]

if __name__ == "__main__":
    for fn in APPROACHES:
        answers = []
        for make in CASES:
            try:
                answers.append(fn(*make()))
            except BaseException as exc:  # a rung that raises names itself
                answers.append(f"!{exc}")
        print(f"{fn.__name__:<38} -> {answers}")
```
