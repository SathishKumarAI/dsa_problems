// rotate-image — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "Reflecting a square across its main diagonal swaps rows with columns. What is left to do after that?",
  "Transpose, then reverse each row, and the composition is a clockwise quarter turn.",
  "Transpose only the cells ABOVE the diagonal — swapping both halves swaps everything back.",
]
