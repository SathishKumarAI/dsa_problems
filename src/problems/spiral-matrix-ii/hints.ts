// spiral-matrix-ii — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "Keep four boundaries — top, bottom, left, right — and walk one side at a time.",
  "After each side, pull that boundary in by one. The walk is always across the current edge.",
  "Check before the two return walks that a row or column is still left, or the last ring writes cells twice.",
]
