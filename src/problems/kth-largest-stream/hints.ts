// kth-largest-stream — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page, and by a journey's story act.

export const hints = [
  "You never care about anything smaller than the current kth largest.",
  "Keep only the k largest values. Which structure evicts its smallest in O(log k)?",
  "A min-heap of size k: its root IS the kth largest.",
]
