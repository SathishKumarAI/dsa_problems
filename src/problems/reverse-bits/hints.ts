// reverse-bits — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "Build the answer bit by bit: shift the result left, then push in the input's lowest bit.",
  "Run the loop exactly 32 times, not until the input runs out — the zeroes have positions too.",
  "For a faster fixed-cost version, swap halves, then quarters, then bytes, then pairs, then single bits: five masked steps.",
]
