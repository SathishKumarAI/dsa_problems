// contiguous-array — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "Equal counts is awkward to track. Is there a relabelling that turns 'equal numbers of two things' into a single number?",
  "Treat 0 as -1. Then a balanced stretch is exactly one whose values sum to zero.",
  "A stretch sums to zero when the running total is the SAME at both ends — so remember the first index at which each running total appeared.",
]
