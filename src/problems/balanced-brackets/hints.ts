// The progressive nudges: a push toward the shape, then the idea, then the
// thing people actually get wrong. Read in order by the problem page and by the
// journey's story act, which shows them one at a time.

export const hints = [
  '"Last opened, first closed" is the literal definition of a stack.',
  "Push openers. On a closer, the top of the stack must be its partner.",
  "Two failure modes: mismatch mid-string, and a non-empty stack at the end.",
]
