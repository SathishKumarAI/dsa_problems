// subsets — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "For each element there are exactly two choices: it is in the subset or it is not.",
  "That makes a binary decision tree of depth n, and every one of its 2^n leaves is a subset.",
  "Record the partial subset at EVERY node, not only at the leaves — or you will only produce the full-length one.",
]
