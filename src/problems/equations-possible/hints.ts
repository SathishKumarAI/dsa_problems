// equations-possible — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "Equality is transitive: a==b and b==c means a and c are stuck together too. What structure keeps track of 'stuck together'?",
  "Do all the equalities first. Once every forced group exists, an inequality is only violated if both of its letters landed in the same group.",
  "Processing the two kinds in one pass cannot work, because an inequality read early has no way to know which merges are still coming.",
]
