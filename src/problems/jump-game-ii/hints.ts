// jump-game-ii — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "Jumping as far as possible each time is NOT the rule — the far landing may be a dead end. Think about levels instead.",
  "From the start you can reach a whole range in one jump. From that range, a wider range in two.",
  "So sweep, tracking where the current level ends and how far the next level reaches. Crossing the boundary costs one jump.",
]
