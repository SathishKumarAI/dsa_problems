// remove-duplicates-sorted — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page, and by a journey's story act.

export const hints = [
  "Because the array is sorted, a duplicate is always sitting next to its twin.",
  "One index reads, one index marks where the next survivor belongs.",
  "Write only when the value differs from the last one written.",
]
