// burst-balloons-arrows — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "An arrow is only ever worth shooting where it bursts as much as possible. Where can that be?",
  "There is never a reason to shoot before the earliest end among the balloons still unburst — shooting later can only catch more.",
  "So sort by end, shoot at the first end you see, and skip every balloon that start is at or before. Then repeat with what is left.",
]
