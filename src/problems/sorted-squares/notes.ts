// sorted-squares — the sections the format has no field for
//
// Converted from docs/deep/sorted-squares_explained.md by scripts/md-to-content.mjs.
// Every byte of prose carried through unchanged; what changed is that the
// STRUCTURE is a type (src/content/types.ts) rather than a heading convention.

import type { Note } from "../../content/types.ts"

export const notes: Note[] = [
  { title: "Comparison", body: `All three allocate an O(n) output array because the problem asks for one; the difference in the
space column is about *auxiliary* memory, of which the last two use none.

---` },
]
