// permutations — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "Unlike subsets, an element skipped now may still be used later, so you cannot walk forward with a start index.",
  "Keep track of which elements are already in the current arrangement and try every one that is not.",
  "Record only when the arrangement is full — a partial ordering is not a permutation.",
]
