// non-overlapping-intervals — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "Removing the fewest is the same as keeping the most. Count what survives and subtract.",
  "If you are building the surviving chain left to right, which interval is the safest one to keep next?",
  "The one that finishes earliest — it leaves the most room for everything after it. That means sorting by END, not by start.",
]
