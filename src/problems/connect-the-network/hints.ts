// connect-the-network — the progressive nudges.
//
// A push toward the shape, then the idea, then the thing people actually get
// wrong. Read in order by the problem page.

export const hints = [
  "First a counting question with no graph in it: how many cables does it take to connect n computers at the very least?",
  "If there are enough cables, the only thing left to know is how many separate groups the current wiring leaves.",
  "Joining k groups into one takes exactly k-1 moves, and every cable that closes a cycle is a spare one you can use.",
]
